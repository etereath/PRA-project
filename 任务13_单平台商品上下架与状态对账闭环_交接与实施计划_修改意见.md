# 任务13交接与实施计划修改意见

- 编制日期：2026-07-24
- 适用文档：`任务13_单平台商品上下架与状态对账闭环_交接与实施计划.md`
- 文档性质：对现有 v0.1 计划的接续修订建议
- 修改原则：保留任务12已验收能力，不覆盖或重写任务12的 v4 改价合同

## 1. 修订结论

任务13继续围绕蚂蚁花团供应商微信小程序，实现单平台商品上架、下架和状态同步闭环。原计划中的共享写锁、严格串行批次、全目标预扫描、UNKNOWN 后停止、唯一 RECONCILE、结果回执和任务14前禁止自动扫描全部 PENDING 等原则继续保留。

原计划需要重点修改以下内容：

1. 商品平台状态由二态调整为三态：`online / offline / manual_review`。
2. `审核中 / 未通过 / 商品未录入` 不再建立独立业务状态，统一进入 `manual_review` 并通知人工。
3. SYNC_STATUS 只完整扫描“上架中”和“待上架”两个页面。
4. T13-0 可以点击“上架”“下架”等入口按钮检查确认窗口，但禁止点击会产生最终副作用的确认或保存按钮。
5. 上架是“修改价格和库存 → 统一回读 → 依次上架 → 最终状态核验”的两阶段写操作。
6. 下架只以目标商品完整退出“上架中”为成功条件，不扫描“待上架”。
7. 下架前必须读取目标商品价格，并将该价格写入数据库和逐商品结果报告。
8. 库存只在成功完成上架后写入正式平台状态表；普通 READ_ONLY、SYNC_STATUS 和下架均不得更新库存。

## 2. 商品状态模型

正式合同只保留以下三种平台商品状态：

| 规范值 | 中文含义 | 自动化处理 |
|---|---|---|
| `online` | 上架中 | 可执行下架；上架任务可判定为已完成 |
| `offline` | 待上架 | 可执行上架；下架后的常规状态 |
| `manual_review` | 待人工介入 | 停止自动写操作，保存证据并通知人工 |

SYNC_STATUS 的状态映射规则如下：

| “上架中”页面 | “待上架”页面 | 同步结果 |
|---|---|---|
| 唯一存在 | 不存在 | `online` |
| 不存在 | 唯一存在 | `offline` |
| 不存在 | 不存在 | `manual_review` |
| 存在 | 存在 | `manual_review` |
| 任一页面身份重复 | 任意 | `manual_review` |

`审核中 / 未通过 / 商品未录入` 的自动化处置完全相同，因此不增加额外业务状态。如果人工证据中能够观察到具体原因，可以写入 `diagnostic_message`，但不得以该说明建立新的自动执行分支。

现有 `listing_status.online_status` 如果继续作为规范状态字段，其数据库约束需要从 `online / offline` 扩展为 `online / offline / manual_review`。

## 3. T13-0 页面探索边界

T13-0 仍然是无业务副作用的页面探索阶段。

允许执行：

- 切换“上架中”和“待上架”标签页；
- 搜索、筛选、分页和滚动；
- 打开商品编辑页或详情页；
- 点击“上架”或“下架”入口按钮以打开确认窗口；
- 读取确认窗口的商品身份、按钮、提示文案和元素结构；
- 点击取消、关闭和返回。

禁止执行：

- 点击上架确认窗口的最终确认按钮；
- 点击下架确认窗口的最终确认按钮；
- 点击会立即保存价格或库存的最终保存按钮；
- 点击任何会实际改变平台商品状态或资料的提交按钮。

如果某个“保存”按钮本身没有二次确认且点击后立即生效，T13-0 只能检查其元素和页面结构，不得点击。

## 4. SYNC_STATUS 定义

SYNC_STATUS 是全商品只读状态盘点，不是上架、下架或 RECONCILE。

执行流程：

```text
读取库存录入中的有效 SKU 清单
→ 登录检查并刷新商品页
→ 完整扫描“上架中”
→ 完整扫描“待上架”
→ 按商品名称和等级映射 internal_sku
→ 校验每个页面内及跨页面身份唯一性
→ 对每个库存 SKU 计算 online / offline / manual_review
→ 原子更新允许更新的状态字段
→ 对 manual_review 生成通知和证据
→ 输出完整同步报告
```

只有两页都确认扫描完整、结束标记可靠时，才能根据“不存在”判定 `manual_review`。如果任一页面读取提前终止、结束标记无法确认或身份枚举不完整，本轮 SYNC_STATUS 整体失败，不得把缺失商品批量改成 `manual_review`。

SYNC_STATUS 不得：

- 点击上架、下架或保存的最终确认按钮；
- 自动创建或执行新的上下架任务；
- 写入正式库存；
- 把控制台显示当作平台状态真值。

SYNC_STATUS 与 RECONCILE 的区别：

| 流程 | 作用范围 | 目的 |
|---|---|---|
| `SYNC_STATUS` | 两个页面中的全部商品 | 获取全量平台现状并发现异常 |
| `RECONCILE` | 某一笔结果不确定的写操作 | 判断该 operation 最终成功、未生效或需要人工处理 |

## 5. 正式上架流程

### 5.1 任务输入

上架任务至少包含：

```text
task_id
platform_name
internal_sku
action_type = set_online
expected_old_status = offline
target_status = online
target_price
target_inventory
```

`action_type` 使用动作值 `set_online`，`target_status` 使用状态值 `online`，不得混用。

### 5.2 执行流程

```text
正常完成登录和 Worker 状态检查
→ 刷新商品管理页
→ 完整扫描“上架中”
→ 完整扫描“待上架”
→ 判断全部目标状态和身份唯一性
→ 按“待上架”页面实时顺序生成执行计划
→ 比较页面价格、库存与任务目标
→ 只修改需要变化的价格和库存
→ 保存每个商品的资料
→ 统一回读全部目标的价格和库存
→ 全部回读一致后，按计划依次点击上架并最终确认
→ 刷新并完整扫描“上架中”
→ 判断每个目标是否成功上架
→ 原子导入结果和允许投影的平台状态
→ 输出批次及逐商品报告
```

### 5.3 执行门禁

- 首次修改价格或库存前，必须完成全部目标的唯一身份门禁。
- 已唯一存在于“上架中”的目标为 `ALREADY_APPLIED`，不得修改资料或重复点击上架。
- 不在两个页面或身份不唯一的目标进入 `manual_review`。
- 价格和库存只修改与任务目标不同的字段。
- 任何目标的价格或库存统一回读失败时，不得开始上架阶段。
- 上架严格按照预扫描得到的页面顺序执行。
- 每次上架后页面行号可能变化，点击下一商品前必须重新按“商品名称 + 等级”动态定位，不能复用旧坐标或旧行号。
- 最终扫描中目标唯一存在于“上架中”，才可判定上架成功。
- 已点击上架但最终未出现在“上架中”的项目进入 `manual_review` 或需要对账的执行状态，不自动重试。

### 5.4 两阶段副作用记录

上架包含“资料保存”和“状态上架”两个写入阶段，账本至少应记录：

```text
DETAIL_SAVE_INTENT_RECORDED
DETAIL_SAVE_CLICKED
DETAIL_VALUES_VERIFIED
ONLINE_INTENT_RECORDED
ONLINE_CLICKED
FINAL_STATE_VERIFIED
```

如果价格和库存已经保存，但上架尚未完成，不能归类为完全未执行。逐商品结果必须明确记录哪些字段已保存、哪些已回读、是否点击最终上架确认，以及最终状态是否验证成功。

## 6. 正式下架流程

### 6.1 任务输入

下架任务至少包含：

```text
task_id
platform_name
internal_sku
action_type = set_offline
expected_old_status = online
target_status = offline
```

下架合同不要求目标价格或目标库存，也不得填入虚假的价格、库存占位值。

### 6.2 执行流程

```text
正常完成登录和 Worker 状态检查
→ 刷新商品管理页
→ 完整扫描“上架中”
→ 确认全部目标唯一存在并记录页面排序
→ 读取并保存每个目标的下架前价格
→ 按页面顺序依次点击下架并最终确认
→ 刷新并重新完整扫描“上架中”
→ 确认全部目标在“上架中”的出现次数为 0
→ 更新数据库
→ 输出批次及逐商品报告
```

下架不扫描“待上架”。目标商品在完整、可靠的后置“上架中”扫描中不存在，即可判定该下架任务完成。

### 6.3 执行门禁

- 首次点击下架前，必须确认全部目标在“上架中”唯一存在。
- 每个目标下架前必须读取并保存当前页面价格。
- 下架严格按照预扫描页面顺序执行。
- 每次下架后页面行号可能变化，下一次点击前必须按商品身份动态重新定位。
- 后置扫描必须完整且结束标记可靠，不能以旧行号定位失败代替“商品已经不在上架中”。
- 下架不修改价格、库存，也不要求读取“待上架”页面。
- 下架成功只更新平台商品状态和下架前价格，库存保持原值。

### 6.4 下架结果字段

逐商品结果至少增加：

```text
observed_price_before_action
price_observed_at
price_source_tab
postcheck_online_occurrences
```

结果示例：

```json
{
  "internal_sku": "AISHA-B-60-Z",
  "action_type": "set_offline",
  "expected_old_status": "online",
  "target_status": "offline",
  "observed_price_before_action": "26.40",
  "price_observed_at": "2026-07-24T10:30:00+08:00",
  "price_source_tab": "上架中",
  "result_status": "VERIFIED",
  "postcheck_online_occurrences": 0
}
```

数据库投影规则：

- 前置扫描确认的下架前价格始终写入本次 operation/attempt 结果，作为审计证据。
- 下架成功时，将 `listing_status.online_status` 更新为 `offline`，将 `current_price` 更新为 `observed_price_before_action`，库存保持原值。
- 下架结果不确定时，保留下架前价格证据，但不得贸然更新商品状态。
- 后续 SYNC_STATUS 如果确认商品既不在“上架中”也不在“待上架”，可以将当前状态更新为 `manual_review`；这不否定此前“退出上架中”的下架目标已经完成。

## 7. 库存写入规则

库存只在成功完成上架后写入正式平台状态表。

允许写入库存必须同时满足：

```text
目标库存已保存
AND 保存后独立回读一致
AND 已点击最终上架确认
AND 最终扫描确认商品唯一存在于“上架中”
```

各流程的库存投影规则：

| 流程或结果 | 是否写入正式库存 |
|---|---|
| READ_ONLY | 否 |
| SYNC_STATUS | 否 |
| SET_OFFLINE | 否 |
| SET_ONLINE 资料已保存但未确认上架成功 | 否 |
| SET_ONLINE 最终确认成功 | 是 |
| `manual_review` | 否 |
| UNKNOWN / NEEDS_RECONCILIATION | 否 |

未满足最终成功门禁时，页面读取值和本次填写值只能保存在 operation、attempt、phase 和结果证据中。

## 8. 数据库和合同前置修改

原计划提出复用 `shadowbot_operations`，但当前任务12账本强制包含价格字段。任务13实施前需要在 Runtime Schema v13 中完成动作泛化：

- 增加 `action_type`；
- 价格字段允许为空；
- 增加 `expected_old_status` 和 `target_status`；
- 上架动作要求 `target_price` 和 `target_inventory`；
- 下架动作不要求目标价格和库存；
- 通过应用层按 `action_type` 校验条件必填字段；
- 不修改任务12 v4 对改价操作的价格门禁。

任务生成器和任务表还需要统一：

```text
action_type = set_online / set_offline
target_status = online / offline
```

不得继续把 `target_status` 写成 `set_online` 或 `set_offline`。

## 9. 结果报告要求

上架和下架都必须输出批次摘要及逐商品自然语言报告。报告至少包括：

- 批次 ID、运行 ID、operation ID、execution attempt ID；
- 动作类型和目标商品数量；
- 前后页面排序；
- 每个商品的 SKU、名称、等级和身份匹配结果；
- 操作前状态、操作后状态和结果；
- 是否修改价格、库存及其回读值；
- 下架前价格；
- 是否点击最终确认；
- 数据库投影结果；
- 失败、UNKNOWN 或 `manual_review` 的简短原因；
- 结果文件、phase 和证据路径及哈希；
- 成功、失败、待人工介入和未尝试计数恒等式。

报告不能只复制 JSON 或代码，必须使用简明自然语言说明本次任务是否成功、每个商品发生了什么，以及哪些事项需要人工处理。

## 10. 实施阶段建议调整

建议将实施顺序调整为：

1. 修订任务13计划和三态状态合同。
2. T13-0：在不点击最终确认或保存按钮的前提下完成页面、编辑页和确认窗口探索。
3. Runtime Schema v13：完成 operation 泛化、状态字段和结果字段。
4. T13-1：实现只读 SYNC_STATUS，只扫描“上架中”和“待上架”。
5. T13-2：实现单商品下架垂直切片及下架前价格入库。
6. T13-3：实现单商品上架的资料修改、统一回读和最终上架闭环。
7. T13-4：实现同动作类型的多商品严格串行批次。
8. T13-5：实现 Web 投影、人工介入通知和可读报告。
9. T13-6：完成受控实机验收、脱敏证据和 CI 复算。

在任务14完成前，正式运行仍必须由操作人员明确选择一个或多个 `task_id`，禁止扫描全部 PENDING 后自动发布。

## 11. 原文格式修正

原计划中的 Markdown 标记被统一转义为 `\#`、`\-`、`\``，导致标题、列表、表格和代码块不能正常渲染。正式修订原计划时应去除这些非必要转义，并使用标准 UTF-8 Markdown。

该格式修正不改变业务语义，但应在下一版文档中一并完成。
