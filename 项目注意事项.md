# 项目注意事项

1. `README.md` 和其他类似说明文本应以中文为主，必要时可补充少量英文术语。
2. 对外展示的使用说明应优先保证中文可读性与操作可执行性。
3. 文档、命令示例和页面说明需要与当前代码实现保持一致，避免文档与功能脱节。
4. 所有业务字段都需要有对应的中文名，用于说明文档、Web 页面和其他面向人工阅读的展示。
5. Excel、代码接口和数据文件中的字段英文名保持不变；中文名用于显示、解释和指引，不直接替换字段键名。

## Web 表单与运营页面设计注意事项

1. 表单布局要优先保证运营人员可读、可录入。不要把早期“网页里的 Excel”继续扩展成日常录入入口；日常入口应使用业务表单，旧 `/tables` 仅作为高级兼容和排障入口。
2. 双栏表单要按“输入控件的 Y 轴坐标”对齐，而不是靠移动某一栏来视觉补偿。每一行左右两侧应采用相同的结构：标题区、控件区、说明区。
3. 如果某一侧有按钮、说明文字或额外操作，另一侧也应通过结构化占位保持控件对齐。不要只给右栏或左栏加 `padding-top` 这种局部偏移来解决对齐问题。
4. 输入框、数字框、下拉框应统一高度、圆角、字体和内边距。新增 `input[type="number"]` 时也要纳入统一控件样式。
5. 字段标题在运营表单中要明显可读，建议使用更大的字号和粗体；但这种样式应限制在对应业务表单范围内，避免影响旧兼容页面。
6. 按钮位置要与相邻输入控件对齐；如果按钮出现在双栏表单右侧，按钮所在区域也应使用和字段一致的标题区/控件区/说明区结构。
7. 修改 Web 样式后，如果浏览器页面没有变化，先确认 Web 服务是否已重启、浏览器是否强刷、当前页面是否来自旧进程。必要时可在 CSS 中加入短注释标记，方便确认当前服务返回的是新模板。

## 商品资料与库存录入注意事项

1. 库存录入是“补充公共库存”，不是简单“新增商品”。同类型商品存在时应优先补库存，不应重复新增 SKU。
2. 同类型库存匹配条件固定为：品种、等级、枝长/规格、单位。平台、成本、是否允许销售和 SKU 不参与匹配。
3. 匹配前必须标准化：品种去除前后空格，等级转为大写字符串，等级 `"0"` 保持字符串，`跟随等级 / follow_grade / FG` 先按等级解析为实际枝长，单位统一为 `扎 / 半扎 / 枝`。
4. 初始库存默认不绑定平台，不拆成平台库存，不因为某个平台常用就把库存默认归属到该平台。
5. SKU 不得包含平台，不得使用随机值生成。新品种应先维护品种代码；未知品种缺少品种代码时，不应静默生成不可读的正式 SKU。
6. 品种录入应使用“选择已有品种 + 新增品种弹窗”的交互。不要在主表单长期暴露“品种代码”输入框；品种代码应只在新增品种时填写，并作为 SKU 生成所需信息。
7. 补充库存时，如果同步更新基础成本和是否允许销售，页面必须明确提示：“同类型商品已存在，本次保存会补充库存，并将基础成本、是否允许销售更新为本次填写值。”
8. 编辑商品资料时，`internal_sku` 默认只读。修改品种、等级、枝长/规格、单位时不自动重写 SKU，并且必须检查是否会与其他商品形成同类型重复。
9. “跟随等级”已有明确映射：`A=65`、`B=60`、`C=55`、`D=50`、`E=45`、`0=0`。新增或编辑商品时应写入解析后的实际枝长；历史数据中的 `跟随等级 / follow_grade / FG` 仍应按等级兼容解析。

## 测试与验收注意事项

1. 业务输入、Web 表单、飞书通知、Mobile Review 等改动完成后，至少运行：
   - `python scripts/run_system_smoke_tests.py`
   - `python -m unittest discover -s tests`
2. Web 展示层的小改动也要跑相关 Web 测试，避免模板语法、登录保护、敏感信息脱敏或旧路由兼容回退。
3. 页面中不得展示 `token=`、raw token、secret、完整 webhook、完整 mobile review URL 或完整本地 runtime DB 路径。
4. 旧入口保留时，要明确它是兼容/高级/排障入口，不要让运营人员误以为旧入口是推荐主流程。
