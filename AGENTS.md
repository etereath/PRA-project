# AGENTS.md — PRA 项目工作指令

PRA 是家庭农场内部使用的鲜切花经营观察、人工决策、可靠执行与恢复系统。当前 Controller 是 Human；当前 Operations Web 属于过渡管理界面，目标正式外部入口为微信小程序 + PRA 后端 API。未来 Agent 仍经既有校验、授权、执行与恢复接口接入。

## 入口与任务范围

1. 新会话先读取本文件、[Owner Product Boundary](docs/owner_product_boundary.md)、[当前状态](docs/project_current_status.md)和用户指定的目标文件或 PR / Issue，确认本轮目标与完成证据。
2. 按任务核验 branch / worktree 和相关版本，保留已有改动；同一任务后续只读增量，复用内容未变的规则、源码和有效证据。
3. 当前任务以用户指定对象为准。除非用户明确要求审核，否则开发 Agent 不运行全量项目审核；文档任务检查直接语义、引用与编码，代码整改检查原问题和直接回归，出现硬安全边界或真实依赖时才扩大。

以下材料按实际涉及内容读取，不是固定必读集：

| 涉及内容 | 入口 |
|---|---|
| 产品实际边界、部署规模、施工减法 | [Owner Product Boundary](docs/owner_product_boundary.md) |
| 产品目标、路线 | [产品概览](docs/project_overview.md) |
| 业务定义、数据权威 | [业务合同](docs/business_contract.md)；与较早企业化/多账号假设冲突时，以 Owner Product Boundary 为准 |
| 既有实现、能力复用 | [实现责任图](docs/rebaseline/task13_6_current_implementation_map.md)及对应 SHA 源码 |
| 跨模块 owner、IG、阶段职责 | [目标职责与缺口](docs/rebaseline/task13_6_target_responsibility_and_gap_matrix.md) |
| WHY / WHEN：范围、投入与停止判断 | [开发范围与效率治理](docs/pra_development_scope_and_efficiency_governance.md)，按需读取相关节 |
| HOW：接口适配、测试、CI 定位与交付步骤 | [开发执行参考](docs/development_workflow.md)，按需读取相关节 |
| 其他主题 | [文档索引](docs/index.md)中对应条目 |

`.archive/` 是人工或网页端参考归档，不作为 Codex 工作规范，默认不检索、不读取；仅用户当前明确点名时使用。Reviewer / Review Governance 材料不通过开发入口间接导入。

## 产品边界与复杂度预算（强制）

- PRA 当前服务约 10 人的家庭农场，实际管理者不超过 3 人；不是 SaaS、多租户或企业通用平台。
- 一个 PRA 部署实例中，每个平台只使用一个受控经营账号。现有 `account_id` 仅可作为兼容实现字段，不得继续扩散为经营 scope、qualification scope、Queue blocker scope、UI 配置或新的 session/page identity 能力。
- 不建设 active session/account binding、page identity proof、企业 IAM、组织目录、复杂审批、Review owner 转交或值班 escalation，除非出现新的真实业务需求并得到负责人明确批准。
- 不同平台由各自 Adapter / Executor / ShadowBot 流程隔离。真实写安全依赖商品读取、expected old 比较、执行和写后回读；页面/商品无法识别时应在副作用前失败，不新增一层页面认证系统。
- 目标正式客户端为微信小程序；当前 Web 功能在小程序替代前继续保持适用认证和 CSRF 等保护，但不得扩成第二套长期企业身份系统。Runtime SQLite、file Queue、ShadowBot Worker 不直接暴露公网。
- 新增表、字段、Service、状态机、Queue、blocker framework、认证层或兼容层前，必须写出当前家庭农场**不加它会发生的具体事故**，并说明现有 Task / operation / attempt / lock / v4/v5 / Worker / Importer / Review 能力为何不能承担。理论上的未来企业需求不构成扩建理由。
- 用户决定、执行责任、当前平台事实、历史证据是四类不同对象。历史 side effect 可以保持 UNKNOWN，而旧 one-shot 当前责任已结束、当前事实合格、新经营决定继续；不得要求四者处处同时完美才允许经营继续。

## 资源、读取与验证预算（强制）

- 按本轮实际交付目标选择证据。推送、归档、停止服务、状态更新和局部文字修改不得自动扩大为代码审计、全仓检查或完整验收；发现范围外问题时先记录并报告，不自行延长任务。
- **运行任何全量回归前，必须先向用户申请并取得本轮明确授权；未获授权不得运行。** 全量回归包括未限定文件或 `-k` 范围的完整 pytest、完整系统冒烟、全仓 Ruff/Mypy/其他静态检查、完整跨平台或实机端到端矩阵，以及主动重新触发完整 CI。申请必须说明定向验证为何不足、预计范围与耗时、已有证据和新增证明价值。
- 用户当前请求已经明确要求完整回归、完整测试、全量验收或完整 CI 时，视为对应验证授权已经取得，不得再次请求确认。该授权只覆盖用户实际要求的验证，不自动扩展到新的实机、部署或真实平台操作。
- 用户授权推送后由 GitHub 自动触发的必需 CI 不需要再次申请；Agent 只能读取该次结果，不得自行重跑。同一 Head 的全量回归最多一次；再次运行必须有新变更或明确失败原因，并重新取得授权。
- 默认只运行当前改动的直接测试与一级依赖。预计超过 4 个测试文件、项目测试量的 20% 或 2 分钟时，视为扩大验证范围并先申请授权。Schema、authority、真实写、授权、事务、并发、UNKNOWN、recovery 或公共持久合同等高风险改动所需证明超过预算时，必须申请扩大验证；未获授权可以完成实现和已授权的定向验证，但必须报告“完整验证待授权/待完成”，不得将定向结果描述为完整 Gate 的等价证明。单个失败用例默认最多重跑一次；不得用大量重复运行碰绿或证明偶发性。
- 文档、状态页、历史分支归档和单纯推送默认不运行 pytest、Ruff 或系统冒烟。历史基线已有失败只记录，不自动修复；测试数量不是完成度指标。
- 新增测试前必须说明对应生产风险、现有测试为何不足以及能否扩展既有 fixture。禁止为增加数量、重复相同状态转换、纯静态文案或历史恢复快照新增测试体系；页面文字若表达 Task 状态、当前责任方、授权结果、风险级别或操作者下一步，属于业务状态合同，可以使用既有测试体系覆盖。
- 全局与当前项目规则每会话只加载一次；内容身份未变时不得重复读取，也不得把全文回显到工具输出。额外 worktree 先比较规则文件身份，相同时复用；不同时只按其实际管辖范围加载一次。
- Git 和搜索默认只取 `status --short`、`diff --stat`、文件名及相关片段；成功日志只保留摘要，失败先取首个相关 traceback。禁止为证明“检查过”而输出完整大型文档、完整 PR 历史、完整 diff、重复进度或无关警告。
- dirty worktree 默认只区分应提交业务文件、明显生成物和无法判断项；无法判断时集中询问一次，不开展跨分支全量比较。简单 Worker 停止默认只做“无活动任务 → 停止信号 → 确认 STOPPED → 清除信号”；只有用户明确要求生命周期验收时才扩展窗口、解释器、部署哈希和完整记录检查。
- 同一 PR 或同一根因的反馈先一次性汇总为冻结清单，按优先级集中整改和一次性定向验证；不得仅追逐最新一条评论反复提交，除非新证据实际改变问题范围。

## 工作原则

- 目标看最新用户裁决、Owner Product Boundary 与业务定义，实现看指定 SHA 源码，验证看相应范围和环境的证据。历史设计可修订，已确认的历史事实不改写。
- 同时考虑业务可行性、安全正确性和维护成本。边界妨碍目标时，提出 BOUNDARY CONFLICT，说明证据、后果和最小替代方案；不静默扩权或增加旁路。
- 跨组件非终态须明确 owner、下一步、触发、失败、重启与收尾；最终确认后的执行责任不能只留在页面、内存或审计日志。
- 新增表、状态机或服务前说明具体事故与既有能力缺口，优先复用 v4/v5、Queue、Worker、Importer、Watchdog、Task、Review/Outbox 和库存 ledger。
- 修改接口、DTO、状态或持久字段时，先查生产与测试调用处并同步适配。开发期默认定向验证；授权、事务、并发和恢复保留必要真实集成，不因父任务规模大就重做全系统验收。
- 正式提交审核或交付前完成与改动风险直接对应的定向门禁；已有等效证据时不重复执行。全量回归、完整冒烟、全仓静态检查和主动 CI 重跑必须遵守上方用户授权门禁；不得因为“准备交付”自动扩大验证范围。
- 小步修改、集中交付；读取局部 diff，正常日志只取摘要，失败再查相关细节。每个主题保留一个主要定义处，不为小修新增长报告或反复提交文档 SHA。

## 硬边界

- 保留主体授权、精确范围、有效期、幂等与原子事务。真实写必须经过“写前读取 → 比较预期旧状态 → 执行 → 写后读取”；外部员工修改是正常经营事实，过时 one-shot 不默认写回。
- UNKNOWN / NEEDS_RECONCILIATION 不得猜测成功或失败，也不得第二次猜测写；优先沿既有唯一 RECONCILE。自动恢复仍无法证明时，人工兜底可以基于“旧执行已停止、旧请求不会再次消费、停止后可信当前事实”结束旧 one-shot；不能仅凭超时释放写锁，也不能把当前事实倒推成历史写入结果。
- PENDING 未授权 Task 不自动执行；后台只接管已有持久最终授权交接的对象。durable handoff 本身是必要安全能力，不因复杂度瘦身删除。
- Observation S4 不继承 Emergency S4 下架权限；新旧业务 authority 不得同时针对同一事实扣减、生成计划或写入权威状态。
- 当前公共商品身份区分 `platform_name`、`internal_sku`、`platform_product_identity`；UI、登录、selector 留在 Adapter / Executor / ShadowBot。`account_id` 不再作为新的公共经营维度。
- 凭据、完整 Review token URL、本地生产配置与真实 Runtime 数据不得进入 Git、公开日志或公开证据。
- 显式编码：普通文本 UTF-8，CSV/TSV UTF-8-SIG，bat ASCII + CRLF；写前检查占用，含中文文件每批写后回读并验证内容。终端显示不作为文件真值；历史原样 archive 和 hash-bound evidence 不为格式或编码整理而重写。

## 权限、交付与收尾

- 用户有效授权在会话内持续，授权范围内的正常步骤不反复确认。commit / push / PR 不等于 merge、结束 Draft、部署、真实 Runtime 或平台操作授权；不修改无关远端状态，不自行扩大权限。
- 开发 Agent 默认只实施用户授权的开发任务；用户明确要求审核时才切换审核职责。按用户指定时机推送，文档修改本身不等于推送授权，现有 CI Gate 也不能自行绕过。
- `docs/project_current_status.md` 只在阶段、范围、blocker、验收结论或其他会影响下一位开发者判断的事实发生**实质变化**时更新；不再要求每一次微小 push、纯文档 SHA 同步或 PR 正文刷新都重复改状态页。即时 Head、commit、CI 结果以 GitHub 为准。发生实质状态变化时仍必须在同一交付中同步状态页。
- 结果只报告当前开发目标、修改、验证和直接限制，不自行宣告 P1 / P2 / Stage Goal 等审核结论。文件、测试、CI、实机及部署证据不互相替代。
- 阶段与历史验收从状态页读取；不重开已完成阶段或套用旧施工限制。cold-start 仅用于明确安排的专项验收，不作为普通开发或文档修改的默认流程。
- 本轮目标与适用验证完成后结束；未涉及的阶段验收、理论问题或无关 CI 失败，不扩张为本轮任务。
