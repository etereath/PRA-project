# AGENTS.md — PRA 项目工作指令

本文件是 PRA 仓库的长期工作入口。

业务主定义、实现事实、当前阶段和审核方法分别由对应 Canonical 文档承担。根 `AGENTS.md` 只保留长期有效的工作方法、硬边界和权限规则，不把历史阶段计划、一次性验收流程或旧实现方式当作永久约束。

---

## 1. 先读什么

### 1.1 每个新会话的最小入口

开始仓库工作时，先读取：

1. `AGENTS.md`
2. `docs/project_current_status.md`
3. 与当前任务直接相关的目标文件、PR / Issue 或用户指令

先确认：

- 当前 main / PR Head；
- 当前任务目标和施工边界；
- 当前代码或文档实际状态；
- 当前任务属于什么 Task Type；
- 本次实际影响对应什么 Review Profile。

不要因为仓库存在大量历史文档，就默认全部读取。

### 1.2 按风险扩展阅读

只有当任务实际涉及相应边界时，再扩展读取：

- 产品目标或路线发生歧义：
  - `docs/project_overview.md`
- 业务定义、日期、数量、权威关系存在疑问：
  - `docs/business_contract.md`
- 需要核对既有实现或复用边界：
  - `docs/rebaseline/task13_6_current_implementation_map.md`
- 涉及 owner、IG、13.7/14 架构责任：
  - `docs/rebaseline/task13_6_target_responsibility_and_gap_matrix.md`
- 涉及审核严重度、复杂度、证据或 Boundary Conflict：
  - `docs/pra_review_risk_and_complexity_governance.md`
- 其他材料：
  - 按 `docs/index.md` 的身份与替代关系读取

对于授权、真实平台写入、数据 authority、Schema / migration、UNKNOWN / RECONCILE、跨组件恢复等高风险任务，必须继续追踪正式生产入口、直接 caller、持久状态和必要集成测试。

对于文案、纯展示、机械文档、局部低风险 helper 等任务，不默认扩大到整个生产调用链。

---

## 2. 工作判断

- PRA 是鲜切花持续观察、人工经营决策、可靠执行与恢复系统。当前 Controller 是 Human / Operations Web；未来 Agent 经过既有校验、授权、执行与恢复接口接入，而不是绕过现有控制面。

- 判断三类事实时分别使用：
  - 业务应该怎样运行：最新用户裁决和 Canonical 业务定义；
  - 代码实际怎样运行：指定 SHA 的源码和真实调用链；
  - 已验证到什么程度：与该版本、环境和范围对应的测试 / CI / 实机证据。

- 历史设计可以被修订，但已经发生的历史事实、原始验收记录和 hash-bound evidence 不得改写。

- 同时考虑三个同级目标：
  - 业务可行性；
  - 安全与正确性；
  - 开发与维护效率。

- 如果既有施工边界妨碍核心目标、必要安全性或合理开发效率，必须显式提出：

  `BOUNDARY CONFLICT`

  说明实际证据、后果和最小替代方案。不得静默扩权，也不得为了机械遵守旧边界不断增加旁路。

- 对跨组件业务对象，应能回答：
  - 当前 owner 是谁；
  - 下一步是什么；
  - 什么条件触发；
  - 失败怎么办；
  - 重启怎么办；
  - 最终如何收尾。

- 合法的人工等待授权可以停留；但最终确认后的执行责任不能只存在于页面、进程内存或审计日志。

- 逻辑责任不等于必须新增表、状态机或 Service。新增复杂度前必须回答：

  > 不增加这一结构，会发生什么具体事故？

  优先复用既有 v4/v5、Queue、Worker、Importer、Watchdog、Review / Outbox、Runtime Task 和库存 ledger。

- Task Type 与 Review Profile 分开判断。父任务很大，不代表所有子修复都自动继承 R4 的全部验证成本。

- 首次实现审核尽量冻结完整 blocker；复审原则上只检查原 blocker、直接回归，以及真正出现的新硬安全 / 架构证据。用户明确要求“完整实现重新审查”时，再重新完整审核。

- 较大实现分别给出：
  - Implementation Review：P1 / P2 / Merge Gate
  - Stage Goal：PASS / FAIL / NOT YET VALIDATED

  两者不得互相代替。

---

## 3. 轻量化开发与验证

### 3.1 增量读取

首次进入一个任务时建立必要的规则、版本和调用链基线。

同一任务后续迭代只重新核验：

- 新 commit；
- changed files；
- 新 PR 评论 / Review；
- 受影响的生产 caller；
- 受影响的测试；
- 发生变化的 Canonical 片段。

已经读取且 SHA / 内容未变化的材料可以复用，不机械重新通读。

新会话不能直接继承无上下文旧结论；仍应读取最小入口并验证当前 SHA。

### 3.2 接口联动

修改以下内容时：

- 函数签名；
- DTO；
- 状态；
- enum；
- 返回结构；
- 持久字段；
- 公共 helper 合同；

先搜索生产调用处和测试调用处，一次列出受影响范围，并同步适配。

避免先修改实现，直到全量回归才发现旧 caller / fixture。

### 3.3 开发期默认定向验证

Bugfix 默认验证：

`原问题稳定复现 → 修复 → 直接回归 → 必要集成`

不因为父任务属于 Integration / R4，就机械重跑整个系统验收。

展示、文案和机械文档修改，不自动要求数据库、Queue、Worker、真实平台或全量系统旅程。

但如果改动实际触及：

- 授权；
- 平台副作用；
- authority；
- 幂等；
- 原子事务；
- concurrency；
- UNKNOWN / RECONCILE；
- migration；
- 恢复责任；

则必须升级验证投入。

### 3.4 稳定候选再跑完整门禁

开发过程中优先定向测试。

准备正式提交审核、阶段交付或合并候选时，在稳定候选提交上完成适用的完整门禁。

如果 GitHub CI 已经在相同源码、相同输入和等效环境覆盖所需全量门禁，默认不再为了形式在本地重复完整全量。

只有以下情况扩大或重跑：

- 最新源码发生实质变化；
- 前一次存在失败；
- 环境不同具有独立价值；
- 有未解除的高风险证据；
- 当前 Gate 明确要求。

报告中简述采用或复用证据的依据。

### 3.5 测试按责任分层

状态枚举、文案、格式化和纯转换优先使用轻量参数化测试。

不要为了测试每一个文案或 DTO 分支，都重复初始化：

- Workbook；
- 完整 Runtime；
- Queue；
- Worker；
- 整条授权链。

但以下边界仍使用必要的真实数据库或集成验证：

- 持久化；
- 身份绑定；
- 幂等；
- Task / operation / continuation 终态；
- 授权；
- 原子事务；
- 并发；
- UNKNOWN；
- 恢复；
- 重启。

组件级正确不能替代需要证明的真实 journey。

### 3.6 文档集中收口

开发中只保留必要的简短变更与验证记录。

交付时统一更新：

- 必要报告；
- PR 说明；
- `project_current_status` 或其他正式入口。

仓库报告绑定已经存在的实现 / 测试 commit。

最终 PR Head 和 CI 链接优先写入 PR 说明，不为了“让文档记录自己最新 SHA”形成无休止的文档 commit 链。

小修、测试 fixture、机械链接或文案调整，不默认新增独立长报告。

历史证据不改写。

### 3.7 Commit 与 Push 分开

允许本地小步 commit。

相关改动和验证形成稳定候选后再集中 push，并遵循用户明确指定的推送时机。

修改过程记录、AGENTS 文案或局部辅助材料，不自动意味着：

- 必须立即 push；
- 必须触发整轮 CI；
- 必须重新开展完整系统验收。

### 3.8 CI 分层

推荐逐步形成：

`开发期轻检 → 纯文档 / 静态检查 → 定向集成 → 正式交付完整 Gate`

同环境、同输入下，已被完整门禁覆盖的测试子集不重复运行。

具有不同平台、不同隔离环境、恢复场景或安全边界价值的检查继续保留。

工作流修改必须属于当前明确任务范围。

本规则本身不自动修改 CI 触发规则，也不得绕过已有 Merge Gate。

### 3.9 工具和日志输出精简

- 可并行的独立检索尽量合并调用；
- 优先读取局部 diff 和直接相关文件；
- 成功日志读取摘要即可；
- 失败时再读取对应 job / stack / artifact；
- 减少无变化轮询；
- 进度更新保持简短；
- 不反复输出整份长文件。

中文和其他非 ASCII 文件仍必须严格 UTF-8 回读验证。

编码正确与业务正确是两个独立结论。

### 3.10 轻量化不得削弱硬约束

轻量化不能成为删除必要检查或虚报完成的理由。

以下边界不得因为“节省成本”而跳过：

- 主体授权；
- 精确任务 / 对象范围；
- 幂等；
- 原子事务；
- UNKNOWN / 唯一 RECONCILE；
- 数据 authority；
- 凭据保护；
- 历史证据完整性；
- 必要编码约束。

始终分别陈述：

- 文件正确；
- 定向测试通过；
- CI 通过；
- 集成旅程通过；
- 真实平台验收通过；
- 部署 / 长期运行通过。

没有哪一项可以自动替代另一项。

---

## 4. 硬边界

- 真实平台写必须经过正式授权链：

  `写前读取 → 比较预期旧状态 → 执行 → 写后读取`

  外部员工直接修改平台属于正常经营事实。过时 Intent 不得默认写回覆盖最新平台事实。

- `UNKNOWN / NEEDS_RECONCILIATION` 不得通过第二次猜测写解决。

  必须沿既有唯一 RECONCILE 或正式人工证据收口。

  UNKNOWN 不得猜测为成功，也不得猜测为失败。

- `PENDING` 未授权 Task 不自动执行。

  Coordinator 只拥有已经形成持久 `execution continuation` 的授权对象。

- Observation S4 不继承 Emergency S4 的自动下架权限。

- 新旧业务 authority 不得同时对同一个真实业务事实扣减、生成计划或写入权威状态。

- 必须区分：
  - `platform_name`
  - `account_id`
  - `internal_sku`
  - `platform_product_identity`

- 平台 UI、登录、selector 等平台细节留在 Adapter / Executor / ShadowBot 边界。

- 凭据、完整 Review token URL、本地生产配置和真实 Runtime 数据不得进入 Git、公开日志或公开证据。

- 开始修改前检查 branch / worktree，并保留已有他人改动。

- 历史原样 archive、hash-bound evidence 和已经确认的历史记录，不为格式、编码或“整理”目的重写。

- CI 不能代替业务验收；安全 fail-closed 也不能代替业务完成。

---

## 5. 当前阶段与历史阶段

当前阶段、开工门槛、已通过事项和下一项工作，**只以 `docs/project_current_status.md` 为动态入口**。

根 `AGENTS.md` 不复制容易过期的阶段施工清单。

长期有效的历史判断：

- Task 13.5 已 `STOPPED / SUPERSEDED`，不得默认继续旧 7G 路线；
- Task 13.6 已完成其重基线与 Canonical 收口职责；
- 13.6 的纯文档限制、cold-start 操作流程、评分过程和历史验收证据属于对应计划 / 报告的历史材料，不作为后续所有任务的重复开工门槛；
- 不因进入后续阶段而重新开展已经通过的 13.6 整轮验收；
- 如果后续修改直接改变 13.6 所建立的 Canonical 业务定义、责任边界或根 AGENTS 关键语义，只对实际受影响部分进行定向复核。

独立 cold-start 仍可作为重大 Canonical / 根工作指令重构后的专门验收手段，但不是普通 Bugfix、功能开发或文档更新的默认流程。

---

## 6. 用户授权与仓库操作权限

- 用户在当前会话中的明确授权优先，并在本会话中持续有效，除非用户撤回或新指令明确替代。

- 创建分支、commit、push 或 PR 的授权，不等于：
  - merge 授权；
  - 结束 Draft 授权；
  - deploy 授权；
  - 修改真实 Runtime 授权；
  - 操作真实平台授权。

- 未经用户明确要求：
  - 不 merge；
  - 不结束 Draft；
  - 不部署；
  - 不操作真实平台；
  - 不修改无关远端分支；
  - 不扩大当前任务权限。

- 审核者默认只审核，不向开发主体分支提交实现，除非用户明确要求其承担修改。

- 最终报告必须明确说明：
  - 是否修改代码 / 文档；
  - 是否 push；
  - 是否结束 Draft；
  - 是否 merge；
  - 是否 deploy；
  - 是否操作真实平台。

---

## 7. 工作停止条件

工程工作的目标不是无限证明系统完美。

当当前任务已经满足：

- 原问题或当前目标已实现；
- 冻结 blocker 已关闭；
- 没有直接回归；
- 适用测试 / CI Gate 已满足；
- 未验证的实机或部署证据已明确标为 `NOT YET VALIDATED`；
- 没有新出现的硬不变量破坏；

应结束本轮开发或审核。

不为了“还能再想到一个理论问题”继续扩大当前任务范围。
