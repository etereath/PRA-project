\# 任务13：单平台商品上下架与状态对账闭环
\#\# 交接与实施计划

\- 文档版本：v0.1
\- 编制日期：2026-07-24
\- 目标仓库：\`etereath/PRA-project\`
\- 前置状态：任务12已完成审查并合并
\- 目标平台：蚂蚁花团供应商微信小程序
\- 当前运行边界：人工明确选择 \`task\_id\` 后受控执行；任务14完成前禁止自动扫描全部 \`PENDING\`

\---

\#\# 1\. 背景与任务定位

任务12已经完成同一平台上的多商品受控改价闭环，包括：

\- 一次批次投递；
\- 页面全目标预扫描；
\- 全批次旧价门禁；
\- 严格串行提交；
\- 每项独立回读；
\- 逐商品 operation、execution attempt 与写锁；
\- UNKNOWN 后唯一只读 RECONCILE；
\- 原子结果导入；
\- 可复核的脱敏实机证据。

当前真实 RPA 的核心写操作仍以 \`UPDATE\_PRICE\` 为主。Mock 平台已具备 \`SET\_ONLINE\`、\`SET\_OFFLINE\` 和 \`SYNC\_STATUS\` 语义，但尚未形成真实微信小程序上的完整闭环。

任务13用于补齐同一平台的商品上下架和状态同步能力，使单平台核心业务动作形成：

\`\`\`text
UPDATE\_PRICE
\+ SET\_ONLINE
\+ SET\_OFFLINE
\+ SYNC\_STATUS
\= 单平台主要业务动作闭环
\`\`\`

任务13完成后，再进入任务14的统一任务审查、授权准入和无人值守调度建设。

\---

\#\# 2\. 核心目标

\#\#\# 2.1 正式支持三类动作

\`\`\`text
SET\_ONLINE
SET\_OFFLINE
SYNC\_STATUS
\`\`\`

定义如下：

| 动作 | 语义 | 是否产生平台副作用 |
|---|---|---:|
| \`SET\_ONLINE\` | 将明确指定的商品切换为上架状态 | 是 |
| \`SET\_OFFLINE\` | 将明确指定的商品切换为下架状态 | 是 |
| \`SYNC\_STATUS\` | 读取平台实际上下架状态并同步到 PRA | 否 |

\`SYNC\_STATUS\` 同时作为日常状态同步能力，以及上下架 UNKNOWN 后的 RECONCILE 基础。

\#\#\# 2.2 复用任务12的安全基础设施

任务13必须复用现有：

\- \`shadowbot\_operations\`
\- \`shadowbot\_execution\_attempts\`
\- \`shadowbot\_side\_effect\_checkpoints\`
\- \`shadowbot\_write\_locks\`
\- result receipt
\- 原子 Importer
\- UNKNOWN
\- 唯一 RECONCILE
\- 脱敏证据导出和 CI 校验

不得另建绕过现有账本和安全边界的简化执行链。

\#\#\# 2.3 共用商品写锁

改价、上架和下架必须共用同一商品写锁身份：

\`\`\`text
write\_identity\_key \= platform\_name \+ internal\_sku
\`\`\`

因此：

\- 商品正在改价时，不允许同时下架；
\- 商品正在上架时，不允许同时改价；
\- 商品存在 UNKNOWN 写锁时，不允许发起任何新的写操作；
\- 只有 RECONCILE 明确得到 \`VERIFIED\` 或 \`NOT\_APPLIED\` 后才能释放锁。

\---

\#\# 3\. 范围边界

\#\#\# 3.1 任务13包含

\- 蚂蚁花团供应商单个平台；
\- 上架列表与下架列表的结构化读取；
\- \`SYNC\_STATUS\` 跨列表只读同步；
\- 单商品 \`SET\_OFFLINE\`；
\- 单商品 \`SET\_ONLINE\`；
\- 同动作多商品严格串行批次；
\- 每项操作后的独立回读；
\- 完整平台状态快照；
\- UNKNOWN 后唯一只读 RECONCILE；
\- Task Center、SQLite 和 Web 状态投影；
\- 自动测试；
\- 受控实机验收；
\- 脱敏证据和 CI 自动核验。

\#\#\# 3.2 任务13不包含

\- 第二个平台；
\- 跨平台混合批次；
\- Agent 自动选择任务；
\- 自动扫描全部 \`PENDING\`；
\- 统一任务审查与授权状态；
\- 无人值守定时发布；
\- 改价与上下架混合在同一个批次；
\- 平台库存数量写入；
\- 批量修改商品基础资料；
\- 通过视觉近似代替结构化身份匹配。

\---

\#\# 4\. 关键架构决策

\#\#\# 4.1 冻结任务12的 v4 改价合同

现有 v4 改价合同已经完成安全审查，应保持稳定：

\`\`\`text
contract\_version \= 4
business action \= UPDATE\_PRICE
\`\`\`

任务13建议新增独立合同：

\`\`\`text
contract\_version \= 5
schema\_version \= shadowbot-listing-action-batch-request-1.0
\`\`\`

对应结果和 phase 可定义为：

\`\`\`text
shadowbot-listing-action-batch-result-1.0
shadowbot-listing-action-batch-phase-1.0
\`\`\`

这样可以：

\- 保持 v4 改价路径稳定；
\- 将上下架特有字段与价格字段分离；
\- 共享底层 operation、attempt、写锁和 RECONCILE；
\- 避免任务13修改破坏已验收的任务12。

\#\#\# 4.2 一个批次只能有一种写动作

允许：

\`\`\`text
一个批次全部 SET\_OFFLINE
\`\`\`

或：

\`\`\`text
一个批次全部 SET\_ONLINE
\`\`\`

禁止：

\`\`\`text
商品A上架
商品B下架
商品C改价
\`\`\`

\`SYNC\_STATUS\` 应独立作为只读批次，不与写动作混合。

\#\#\# 4.3 第一版保持严格串行

即使平台存在复选框或批量上下架按钮，任务13第一版仍应采用：

\`\`\`text
一次批次投递
→ 一次跨列表预扫描
→ 按实时页面顺序
→ 逐商品严格串行
→ 每项独立回读
\`\`\`

原因是只有严格串行才能可靠判断：

\- 哪个商品已产生副作用；
\- 哪个商品尚未开始；
\- 哪个商品处于 UNKNOWN；
\- 哪些后续商品必须停止。

平台原生批量按钮可在未来独立优化任务中评估，不应进入任务13首版。

\---

\#\# 5\. 任务数据模型

\#\#\# 5.1 正式任务字段

建议使用：

\`\`\`text
task\_id
platform\_name
internal\_sku
action\_type
expected\_old\_status
target\_status
expires\_at
\`\`\`

示例：

\`\`\`json
{
  "task\_id": "TASK-...",
  "platform\_name": "蚂蚁花团供应商",
  "internal\_sku": "AISHA-B-60-Z",
  "action\_type": "set\_offline",
  "expected\_old\_status": "online",
  "target\_status": "offline"
}
\`\`\`

\#\#\# 5.2 规范状态值

核心合同只使用：

\`\`\`text
online
offline
\`\`\`

平台 Adapter 负责将 UI 文案映射为规范值，例如：

\`\`\`text
上架中  \-\> online
已下架  \-\> offline
待上架  \-\> offline
\`\`\`

具体映射必须通过 T13-0 现场探索确认，不能预先假设。

\#\#\# 5.3 是否包含价格字段

第一版原则：

\`\`\`text
SET\_ONLINE / SET\_OFFLINE 只修改 online\_status
不主动改变 current\_price
\`\`\`

但必须在 T13-0 确认：

\- 上架是否强制重新填写价格；
\- 上架是否保留历史价格；
\- 上架是否需要库存或其他必填字段；
\- 下架是否需要确认原因。

若平台强制输入价格，v5 item 才增加：

\`\`\`text
expected\_price
preserved\_price
\`\`\`

不能直接复用 v4 的 \`target\_price\` 语义，避免把“保留原价”和“修改价格”混为一谈。

\---

\#\# 6\. 状态门禁与结果规则

\#\#\# 6.1 SET\_OFFLINE

前置条件：

\`\`\`text
expected\_old\_status \= online
target\_status \= offline
\`\`\`

| 实际状态 | 处理 |
|---|---|
| \`online\` | 允许执行下架 |
| \`offline\` | \`ALREADY\_APPLIED\`，只读确认，不重复点击 |
| 状态与任务不一致 | \`STATE\_CHANGED / NOT\_STARTED\` |
| 无法唯一定位 | \`FAILED / NOT\_STARTED\` |
| 点击后无法确认 | \`UNKNOWN\` |

\#\#\# 6.2 SET\_ONLINE

前置条件：

\`\`\`text
expected\_old\_status \= offline
target\_status \= online
\`\`\`

| 实际状态 | 处理 |
|---|---|
| \`offline\` | 允许执行上架 |
| \`online\` | \`ALREADY\_APPLIED\`，只读确认，不重复点击 |
| 状态与任务不一致 | \`STATE\_CHANGED / NOT\_STARTED\` |
| 无法唯一定位 | \`FAILED / NOT\_STARTED\` |
| 点击后无法确认 | \`UNKNOWN\` |

\#\#\# 6.3 ALREADY\_APPLIED

\`ALREADY\_APPLIED\` 必须基于实时平台观察：

\`\`\`text
平台当前已处于 target\_status
→ 不产生新副作用
→ 保存观察证据和 observed\_at
→ operation 归并为 VERIFIED/ALREADY\_APPLIED
\`\`\`

不能仅依赖 PRA 旧缓存判断已完成。

\#\#\# 6.4 批次门禁

在首个写操作前必须完成：

\- 所有目标均存在；
\- 每个目标身份唯一；
\- 每个目标当前状态可读取；
\- 每个目标满足 \`expected\_old\_status\`，或已处于目标状态；
\- 页面快照完整；
\- 批次内无重复页面身份；
\- 批次内无重复写入身份；
\- 所有任务仍为人工明确选择且发布前有效。

任何目标出现无法解释的状态时，全批次在副作用前停止。

\---

\#\# 7\. UNKNOWN 与 RECONCILE

\#\#\# 7.1 UNKNOWN 场景

例如：

\`\`\`text
点击“下架”
→ 页面刷新失败
→ 无法确认商品是否已进入下架列表
\`\`\`

此时必须记录：

\`\`\`text
item.status \= UNKNOWN
operation.status \= NEEDS\_RECONCILIATION
write\_lock.status \= UNKNOWN
task.status \= MANUAL\_REVIEW
后续商品 \= NOT\_ATTEMPTED
batch.status \= UNKNOWN
\`\`\`

\#\#\# 7.2 RECONCILE 必须只读

唯一 RECONCILE 只允许：

\- 读取上架列表；
\- 读取下架列表；
\- 按商品名称 \+ 等级定位；
\- 判断目标实际在哪个列表；
\- 记录观察时间和证据。

禁止：

\- 再次点击上架；
\- 再次点击下架；
\- 自动补偿写操作；
\- 重新创建 COMMIT；
\- 自动释放 UNKNOWN 写锁。

\#\#\# 7.3 RECONCILE 结果

| 实际状态 | 对账结果 |
|---|---|
| 等于 \`target\_status\` | \`VERIFIED\`，释放写锁 |
| 等于 \`expected\_old\_status\` | \`NOT\_APPLIED\`，释放旧 operation 的写锁 |
| 同时出现在两个列表 | \`UNKNOWN\`，保持写锁 |
| 两个列表均不存在 | \`UNKNOWN\`，保持写锁 |
| 身份不唯一 | \`UNKNOWN\`，保持写锁 |
| 页面读取不完整 | \`UNKNOWN\`，保持写锁 |

如果得到 \`NOT\_APPLIED\`，是否重新发起写操作必须由新的任务或后续人工授权决定，不能由 RECONCILE 自动重试。

\---

\#\# 8\. 跨列表状态同步

\#\#\# 8.1 SYNC\_STATUS 流程

\`\`\`text
打开商品管理
→ 刷新并读取“上架中”
→ 刷新并读取“已下架/待上架”
→ 合并为完整 platform listing snapshot
→ 校验身份唯一性
→ 原子更新 listing\_status
\`\`\`

\#\#\# 8.2 每个商品的同步字段

\`\`\`text
platform\_name
product\_name
grade
internal\_sku
online\_status
current\_price
inventory
observed\_at
source\_tab
row\_identity
\`\`\`

\#\#\# 8.3 全局一致性规则

同一业务身份：

\`\`\`text
platform\_name \+ product\_name \+ grade
\`\`\`

在完整快照中必须唯一。

以下情况必须产生同步告警或 Review 候选：

\- 同一商品同时出现在上架和下架列表；
\- 同名同等级出现多条；
\- 正式映射商品在两个列表均找不到；
\- 列表读取提前终止；
\- 页面总数或结束标记无法确认；
\- 状态文案不能映射为规范状态；
\- 页面商品身份与 \`products.xlsx\` 映射冲突。

\#\#\# 8.4 人工运营导致的跨运行状态变化

当前平台需要继续支持正常人工运营。两个独立运行之间出现商品上下架状态、价格、库存或页面顺序变化，属于允许的外部事实变化，不应仅因“与上一次快照或开发预期不同”就判定为自动化故障、写入 UNKNOWN 或创建普通异常 Review。

边界冻结如下：

\- 每个完整 \`SYNC\_STATUS\` 都是带观察时间的时间点事实，不是对后续平台状态的持续锁定；
\- 旧快照、旧页面行号和基于旧快照生成但尚未发布的开发提案，在观察到平台变化后必须失效，不得继续作为 COMMIT 授权依据；
\- 新写操作必须重新执行当次全目标预扫描，并按当时页面事实进行身份、状态、价格和库存门禁；
\- 若当次预扫描在首次写入前发现状态或资料不符合合同，应整批零写停止，明确区分 \`NOT\_APPLIED\` 与 \`NOT\_ATTEMPTED\`，并释放可安全释放的写锁；这属于正常并发防护，不是执行器故障；
\- 只有完整扫描本身发现 \`both / neither / ambiguous\`、未映射身份、重复身份或结束标记不完整时，才按本节既有一致性规则产生异常事实；
\- 最终确认点击后无法证明副作用结果时，仍必须进入 \`UNKNOWN\` 并保留写锁；不得以“可能是人工改动”降低为普通外部变化。

\---

\#\# 9\. 数据库设计建议

Runtime Schema 建议升级到 v13。

\#\#\# 9.1 新增表

\`\`\`text
shadowbot\_listing\_action\_batches
shadowbot\_listing\_action\_batch\_items
shadowbot\_listing\_result\_receipts
\`\`\`

继续引用现有：

\`\`\`text
shadowbot\_operations
shadowbot\_execution\_attempts
shadowbot\_side\_effect\_checkpoints
shadowbot\_write\_locks
\`\`\`

\#\#\# 9.2 批次表建议字段

\`\`\`text
batch\_id
contract\_version
execution\_profile
platform\_name
action\_type
manifest\_sha256
instruction\_hash
execution\_attempt\_id
result\_id
status
created\_at
updated\_at
\`\`\`

\#\#\# 9.3 批次项建议字段

\`\`\`text
batch\_id
item\_id
source\_task\_id
operation\_id
item\_execution\_attempt\_id
action\_type
internal\_sku
expected\_product\_name
expected\_grade
expected\_old\_status
target\_status
write\_identity\_key
page\_identity\_key
preflight\_status
preflight\_observed\_at
action\_intent\_at
action\_clicked\_at
readback\_status
readback\_observed\_at
action\_attempted
side\_effect\_state
status
error\_code
error\_message
updated\_at
\`\`\`

\#\#\# 9.4 状态枚举建议

逐项结果：

\`\`\`text
NOT\_ATTEMPTED
VERIFIED
ALREADY\_APPLIED
NOT\_APPLIED
FAILED
UNKNOWN
\`\`\`

副作用状态：

\`\`\`text
NOT\_STARTED
ACTION\_INTENT\_RECORDED
ACTION\_CLICKED
VERIFIED
NOT\_APPLIED
UNKNOWN
\`\`\`

批次终态应统一由逐项结果推导：

\`\`\`text
全部 VERIFIED / ALREADY\_APPLIED
→ VERIFIED

存在任何 UNKNOWN
→ UNKNOWN

无 UNKNOWN，成功与明确失败混合
→ PARTIAL

全部明确失败或未执行
→ FAILED
\`\`\`

\---

\#\# 10\. 实施阶段

\#\#\# T13-0：页面结构探索

只做 READ\_ONLY，不执行真实上下架。

需要确认：

\- 上架中列表入口与页面结构；
\- 下架列表的实际名称；
\- 两个列表的结束标记；
\- 上架和下架按钮位置；
\- 单商品按钮还是复选框；
\- 是否存在确认弹窗；
\- 上架是否要求价格、库存或其他字段；
\- 操作后商品进入哪个列表；
\- 状态更新是否存在延迟；
\- 商品名称和等级是否在两个列表稳定可读；
\- 是否存在搜索、筛选或分页；
\- 是否存在同名同等级但不同规格的商品。

交付物：

\`\`\`text
docs/plans/task13\_ui\_discovery\_report.md
脱敏截图
READ\_ONLY request/result/phase
选择器与结构索引清单
页面状态映射表
风险清单
\`\`\`

本阶段不得点击真实上架或下架按钮。

\#\#\# T13-1：状态模型与 v5 合同冻结

冻结四维状态、统一写锁、action gate、v5 request/result/phase 和两阶段副作用模型。

\#\#\# T13-2：Runtime Schema v13

完成 v12→v13 无损迁移、公共批次注册表、快照父子表、异常事实表、通用 operation 与历史 v4 回归。

\#\#\# T13-3：跨列表只读同步

实现正式 \`SYNC\_STATUS\`：

\- 一次读取“上架中”和“待上架”；
\- 合并完整状态快照；
\- 校验身份唯一性；
\- 使用真实 \`observed\_at\`；
\- 原子更新 \`listing\_status.online\_status\`；
\- 原子创建或清除异常、Review 和通知；
\- 不产生平台写操作；
\- 生成脱敏 request/result/phase/receipt/ACK 和数据库回读证据；
\- 由 CI 独立复算合同、绑定关系和计数恒等式。

\#\#\# T13-4：单商品上架垂直切片

先实现 \`SET\_ONLINE\`，为下一阶段提供同一商品的确定在线起点。不得假设可以机械复用下架动作。

目标必须来自一次有效完整 \`SYNC\_STATUS\`，且唯一处于 \`waiting\_only\`、无开放异常 Review、无阻断写锁。开发实机测试在创建 COMMIT 前按固定批次清单取得一次授权。

覆盖：

\- 全目标门禁和页面身份唯一性；
\- 价格、库存修改与分别回读；
\- 最终上架确认；
\- 上架后状态、价格和库存独立回读；
\- 已经上架且资料一致时 \`ALREADY\_APPLIED\`；
\- 在线但资料不一致时整批首次写入前阻断；
\- 点击前失败；
\- 资料阶段部分生效；
\- 最终点击后 UNKNOWN；
\- RECONCILE → VERIFIED / NOT\_APPLIED / PARTIALLY\_APPLIED；
\- 写锁冲突。

本阶段必须记录本次验证商品的 \`internal\_sku\`、页面身份、上架后价格、库存、operation ID 和最终快照；T13-5 必须复用同一件商品。

\#\#\# T13-5：同一商品下架垂直切片

只有 T13-4 的目标商品达到 \`VERIFIED\` 后才能进入本阶段。不得换用其他商品绕过该前置条件。

对 T13-4 记录的同一 \`internal\_sku\` 执行 \`SET\_OFFLINE\`：

\- 完整扫描“上架中”并确认身份唯一；
\- 在点击前记录该商品的页面价格并写入数据库证据；
\- 点击下架并在确认窗口执行最终确认；
\- 完整回读“上架中”，目标出现次数为 0 时判定 \`VERIFIED\`；
\- 已不在“上架中”时 \`ALREADY\_APPLIED\`；
\- 身份不唯一或扫描不完整时首次写入前阻断；
\- 点击前失败；
\- 点击后 UNKNOWN；
\- RECONCILE → VERIFIED / NOT\_APPLIED；
\- 写锁冲突。

这两个阶段组成同一商品的状态往返证据：

\`\`\`text
waiting_only
→ SET_ONLINE VERIFIED
→ online
→ SET_OFFLINE VERIFIED
→ not_online
\`\`\`

\#\#\# T13-6：多商品严格串行批次

分别支持：

\`\`\`text
多商品 SET\_OFFLINE 批次
多商品 SET\_ONLINE 批次
\`\`\`

要求：

\- 一次投递；
\- 全目标预扫描；
\- 全批次状态门禁；
\- 按实时页面位置执行；
\- 每项独立回读；
\- 首个 UNKNOWN 后停止；
\- 后续项 \`NOT\_ATTEMPTED\`；
\- 完整 item 结果；
\- 原子 Importer；
\- result receipt 和 ACK；
\- 可重放但不可重复写平台。

\#\#\# T13-7：Web 与运营投影

Tasks 页面增加：

\- 动作类型；
\- 预期旧状态；
\- 目标状态；
\- 实际回读状态；
\- 批次 ID；
\- operation ID；
\- execution attempt；
\- UNKNOWN；
\- RECONCILE 状态；
\- observed\_at；
\- 错误代码和人工处理入口。

不得在此阶段增加自动审批或自动调度。

\#\#\# T13-8：受控实机验收

至少完成：

1\. 一个商品正常上架并达到 \`VERIFIED\`；
2\. 对第 1 项的同一 \`internal\_sku\` 正常下架并达到 \`VERIFIED\`；
3\. 多商品正常上架与多商品正常下架；
4\. 已处于目标状态的 \`ALREADY\_APPLIED\`；
5\. 全批次前置状态不一致，0 次点击；
6\. 第一个成功、第二个 UNKNOWN、后续项停止；
7\. UNKNOWN → 唯一 RECONCILE → VERIFIED；
8\. UNKNOWN → 唯一 RECONCILE → NOT\_APPLIED；
9\. 改价与上下架共用写锁的冲突测试；
10\. phase/result 写入异常恢复；
11\. 脱敏证据进入仓库并由 CI 复算。

\---

\#\# 11\. 自动测试矩阵

\#\#\# 11.1 合同测试

\- manifest 哈希稳定；
\- request instruction hash 稳定；
\- item ID、operation ID 和 attempt ID 绑定正确；
\- action type 不能被覆盖；
\- 一个批次只能有一种动作；
\- 严格布尔和状态类型检查；
\- 顶层语义由 items 唯一推导；
\- UNKNOWN 优先级高于 PARTIAL。

\#\#\# 11.2 发布事务测试

\- operation、attempt、写锁、batch 和 task 在一个事务内创建；
\- 任一项失败全部回滚；
\- ACTIVE/UNKNOWN 写锁阻止改价、上架和下架；
\- 明确未发布时释放锁并恢复任务；
\- 发布边界不确定时全部进入 UNKNOWN。

\#\#\# 11.3 Worker 测试

\- 全量结果骨架始终与 request 等长；
\- 首个 UNKNOWN 后停止后续项；
\- 通用异常从 phase 恢复；
\- phase 缺失或损坏时 fail-closed；
\- 超大结果保留完整 item 集合；
\- ALREADY\_APPLIED 不产生点击；
\- RECONCILE 不包含写动作。

\#\#\# 11.4 Importer 测试

\- 完整预校验后单事务写入；
\- 任一 item 无效时数据库无部分更新；
\- 状态观察使用真实 \`observed\_at\`；
\- UNKNOWN 不覆盖已确认状态；
\- result receipt 幂等；
\- 冲突结果进入 quarantine；
\- quarantine 后 Watchdog 可以恢复；
\- 已接受结果不能被后续恶意文件回滚。

\#\#\# 11.5 证据测试

\- 成功上下架批次；
\- UNKNOWN→RECONCILE；
\- request/result/phase/manifest 绑定；
\- 逐项执行顺序；
\- 平台状态前后变化；
\- 原始归档 SHA 与脱敏文件 SHA 分别记录；
\- CI 自动运行验证脚本。

\---

\#\# 12\. 任务14前的运行边界

任务13继承任务12的临时授权规则：

1\. 操作人员必须明确传入一个或多个 \`--task-id\`；
2\. \`PENDING\` 只表示候选任务；
3\. 不允许调度器自动扫描全部 \`PENDING\`；
4\. 不允许 Agent 自动发布 COMMIT；
5\. 不允许 \`SYNC\_STATUS\` 自动触发写操作；
6\. 统一任务审查、授权状态和无人值守调度延期到任务14；
7\. Task13 的 Web 页面只能提供选择和审计，不得把列表加载等同于执行授权。

\---

\#\# 13\. 完成定义

任务13只有同时满足以下条件才能标记完成：

\- \`SET\_ONLINE\`、\`SET\_OFFLINE\`、\`SYNC\_STATUS\` 三种语义稳定；
\- 上架与下架页面均可结构化读取；
\- 单商品上架和下架均通过实机验证；
\- 多商品批次保持严格串行；
\- 改价和上下架共享同一 SKU 写锁；
\- 所有 UNKNOWN 保持锁并进入唯一只读 RECONCILE；
\- Importer 使用完整预校验和单事务写入；
\- \`listing\_status\` 使用真实观察时间；
\- Worker 最外层异常能够从绑定 phase 恢复；
\- Windows、Linux、wheel 和 smoke CI 全绿；
\- 正常批次和 UNKNOWN 批次均具有可复核脱敏证据；
\- 任务14前的人工 \`task\_id\` 选择边界继续有效；
\- 权威状态文档已更新；
\- 审查通过后方可标记任务13完成。

\---

\#\# 14\. 优先级与依赖

| 维度 | 评价 |
|---|---|
| 业务优先级 | P0 |
| 开发复杂度 | 高 |
| UI 不确定性 | 高 |
| 架构复用程度 | 高 |
| 对任务14的依赖 | 无 |
| 任务14对本任务的依赖 | 高 |
| 实机风险 | 中高 |
| 推荐模型 | 高推理能力代码 Agent \+ 人工实机操作 |

推荐顺序：

\`\`\`text
任务12：多商品改价闭环                    已完成
任务13：上下架 \+ 状态同步                 当前任务
任务14：统一任务审查与执行准入
任务15：受控自动调度与运营编排
\`\`\`

\---

\#\# 15\. 第一阶段 Codex Goal 建议

任务13的第一个开发 Goal 应严格限制为：

\`\`\`text
仅完成 T13-0 页面结构探索与只读证据采集。

不得执行真实上架或下架；
不得新增自动调度；
不得修改任务授权语义；
不得重构任务12 v4 改价合同；
不得将页面行号作为持久业务身份。

需要输出：
1\. 上架中与下架列表结构报告；
2\. 状态文案到 online/offline 的映射；
3\. 上架与下架操作入口及确认弹窗结构；
4\. 上架是否强制价格/库存等字段；
5\. 脱敏 READ\_ONLY request/result/phase 和截图；
6\. v5 合同字段建议；
7\. 风险和待人工确认事项。
\`\`\`

只有 T13-0 报告经过人工审查后，才进入 T13-1 和真实写操作开发。
