# 任务 13.5-0：Web 现状独立审计快照

- 审计时间：2026-07-29 02:56:06.335（Asia/Shanghai）
- 审计方式：Codex 内置浏览器，只读浏览；未提交业务表单，未触发任务生成、复核、
  通知测试或平台动作
- 代码基线：`main` / `268c4ed9facd1ec39698843301d31fff9e860495`
- 远端基线：审计时 `main == origin/main`
- 测试角色：`admin`
- 浏览器：Codex In-app Browser
- 桌面视口：1440 × 900
- 移动视口：390 × 844
- Runtime Schema：v13
- Runtime DB 脱敏快照标识：
  `runtime-db-sha256:4878131a1b76ce7078a51fc8659aaa14583dbb89287b6f52bed44a442db7c723`
- 快照方法：通过 SQLite 在线只读连接备份到内存，再对序列化快照计算 SHA-256；
  文档不记录数据库绝对路径
- 数据库自检：`quick_check=ok`、外键错误 0、30 张表、序列化快照 1,454,080 字节

本文件是 Issue #20 要求的独立 Web 审计文档。任务数、页面高度、DOM hash 和文件体积
都是上述时间与数据快照下的事实，不是长期常量，也不是未来验收必须维持的固定数值。

## 1. 路由清单

### 1.1 当前七个全局导航入口

| 入口 | 路由 | 当前用途 |
| --- | --- | --- |
| 首页总览 | `/dashboard` | 待复核、失败通知、待执行和过期摘要 |
| 任务中心 | `/tasks` | 任务列表、详情、脚本状态和 Mock 测试页签 |
| 复核中心 | `/reviews` | Review 列表、详情和处理表单 |
| 通知中心 | `/notifications` | 通知发送记录 |
| 执行记录 | `/execution-logs` | 队列状态、执行日志和原始输出 |
| 业务数据 | `/business-inputs` | 商品、库存、规则、产能和冷库录入 |
| 生成待处理任务 | `/task-generator` | 规则选择、预览和任务生成 |

### 1.2 其他现行路由

| 路由 | 类型 | 处理原则 |
| --- | --- | --- |
| `/health` | 无认证健康探针 | 保留稳定协议 |
| `/system` | 系统检查 | 升级为“系统维护”一级入口 |
| `/system/test-feishu-notification` | 有外部效果的 POST | 保留认证、CSRF、目标和环境确认 |
| `/runtime/login`、`/runtime/logout` | 登录与会话 | 保留安全行为；迁入 `auth.py` |
| `/runtime` | 旧运行态聚合页 | 只读兼容或隐藏 |
| `/mobile/review/{token}` | Mobile Review | 保留 token、原子事务和失效语义 |
| `/` | 旧任务生成入口 | 兼容迁移到正式运营入口 |
| `/tables` | 旧 Excel 管理 | 降级为管理员高级工具 |
| `/execution` | 旧执行入口 | 只读兼容到执行审计 |
| `/manual-intervention` | 旧人工介入 | 只读兼容到“待处理” |

代码路由清单来自 `app/web.py` 的 WSGI 分发；浏览器实测集中覆盖七个全局入口。旧路由
不再承担新业务信息架构，但在 13.5-8 完成兼容测试前不得直接删除。

## 2. 浏览器证据

### 2.1 桌面端 1440 × 900

| 路由 | 页面高度 | 主表结构 | 快照事实 | DOM 摘要 SHA-256 |
| --- | ---: | --- | --- | --- |
| `/dashboard` | 928 px | 无主表 | 14 个链接；首页仍是状态计数 | `9518083a4c9ebeb14da6ec04d405c224de6f916ad69cac53b4ce05ba85a512d5` |
| `/tasks` | 5,330 px | 12 列、50 行、约 1,335 px 宽 | 共 146 条，第 1/3 页 | `b71b3f571d9a48d3732934a02221de515c3da78c30a36d68512d5e687205dc93` |
| `/reviews` | 5,152 px | 7 列、50 行、约 1,335 px 宽 | 共 53 条，第 1/2 页 | `1dd95ee25b3ae9eb9853442bf8bcef479b1e7dacd9b8d60bd833d53e3b3c350d` |
| `/notifications` | 3,829 px | 7 列、41 行、约 1,335 px 宽 | 共 41 条 | `53adf2e48f70ab5d0d41c30b2535139adf0cb15df34f67a0c556de3a465ab8fd` |
| `/execution-logs` | 31,704 px | 8 列、32 个顶层日志、32 个嵌套表 | 嵌套后共有 684 个后代行；主表约 1,702 px 宽 | `2460f2da89ef2e000d266633dc765b82fc02be31ff7a92f6bef79cb0b5b4f2ef` |
| `/business-inputs` | 2,136 px | 9 列、12 行；16 个表单 | 每条商品预渲染编辑表单 | `f8c93de42ec09f1a6e4086793659aaf38ba75a69d59d29aeece15f24cb8385ea` |
| `/task-generator` | 1,126 px | 无主表 | 规则预览入口仍是一级导航 | `caa4d61385b468af3f3fca35c6bb08ba6b6c27e9f1196500d83cb53b6ca051a6` |

Runtime DB 同一快照中的相关计数为：`tasks=146`、`review_tasks=53`、
`notification_logs=41`、`execution_logs=32`、`listing_status=9`、
`listing_sync_snapshots=15`。执行页的 684 行是 32 个顶层执行日志递归展开后的 DOM
后代行数，不能误写成 684 条数据库执行日志。

### 2.2 移动端 390 × 844

| 路由 | 页面高度 | 导航高度 | 首个表格 | 结论 |
| --- | ---: | ---: | --- | --- |
| `/dashboard` | 1,244 px | 169 px | 无 | 导航连续占用首屏，核心运营信息下移 |
| `/tasks` | 6,844 px | 169 px | 12 列、约 1,239 px 宽 | 表格远宽于 358 px 主内容区 |
| `/execution-logs` | 31,887 px | 169 px | 首表约 640 px 宽 | 原始日志不适合移动端默认加载 |
| `/business-inputs` | 3,224 px | 169 px | 9 列、约 644 px 宽 | 表单与表格需要按需渲染和卡片化 |

移动端和桌面端 DOM 摘要相同，说明当前主要依靠 CSS 重排，没有为移动端裁剪信息层级
或减少默认加载内容。

## 3. 代码快照

| 文件 | 字节 | 行数 | SHA-256 |
| --- | ---: | ---: | --- |
| `app/web.py` | 383,965 | 8,000 | `9b0b689c68aa0c6d2c8372e2fcfc750288fea7c247a6ae8ea9d158e36b20fdd7` |
| `app/web_styles.py` | 15,034 | 580 | `5f6a626dc6571e8c40dc6498cd9d4a3f5cb301e7b57a4998577724c631283017` |

`app/web.py` 同时承担 WSGI 分发、认证、CSRF、表单解析、业务调用、HTML 拼接、运营
展示和技术诊断。继续在单文件中增加自动化、销售和 Incident 页面会扩大安全与回归面。

## 4. 主要问题与工程裁决

1. 当前导航按开发模块排列，缺少 Issue #20 冻结的“今日运营、平台商品、销售分析、
   自动化、待处理、任务中心、业务资料、系统维护”八个一级入口。
2. 列表把业务摘要、内部枚举、复合 ID、hash、phase 和原始 JSON 混在同一层；
   运营人员难以快速判断影响和下一步。
3. 任务、Review 和执行详情附在完整列表或递归输出之后，导致页面高度、DOM 规模和
   移动端成本随历史数据增长。
4. Web 仍缺少 Automation Service 的计划、运行、租约、最近成功、数据新鲜度和
   只读补跑入口；自动规则 apply 仍依赖 CLI。
5. 业务资料 URL 和页面中出现本地路径；重写后 URL 只能携带业务筛选和稳定记录 ID。
6. `Review` 与 `Incident` 必须在“待处理”中协作展示，但不能合并为一个状态机。
7. 本文仍是 2026-07-29 的现状证据，不再决定迁移方式。2026-08-07 已改为新建
   `app/operations_web/` 并直接替代旧 Web，不保留渐进兼容层。
8. 八个一级入口和运营 UI 直接纳入 13.5-7；在相应后端合同落地前，页面不得用假数据
   占位并冒充真实经营事实。

## 5. Web 开工门禁

- [x] 已记录精确审计时间、main SHA、Runtime DB 脱敏快照、浏览器、视口和测试角色。
- [x] 已记录现行路由、桌面/移动端规模和 DOM 摘要 hash。
- [x] 已区分数据库顶层执行日志与页面递归后代行，避免错误数据口径。
- [x] 当前 `main` 已建立
  `checkpoint/pre-task13-5-7-web-rewrite-20260807`，不再冻结旧路由兼容合同。
- [ ] 13.5-7 编码前冻结登录/Session/CSRF 安全属性、wheel 资源清单和正式 Presenter
  输入；不要求旧 HTML 或路由行为一致。
- [ ] Web 写操作继续只调用应用服务；不得直接写 SQL、拼 ShadowBot 队列或提供
  `SYSTEM_EMERGENCY` 手工旁路。
