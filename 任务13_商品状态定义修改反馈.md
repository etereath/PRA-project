# 任务13商品状态定义修改反馈

- 编制日期：2026-07-24
- 适用范围：任务13单平台商品上架、下架、SYNC_STATUS、人工介入和写操作对账
- 文档性质：对商品状态模型讨论的结论反馈
- 修改边界：本文件不修改任务12的 v4 改价合同，也不改变任务14前必须明确选择 `task_id` 的运行边界

## 1. 讨论结论

原修改意见将商品状态定义为：

```text
online / offline / manual_review
```

该定义混合了两个不同概念：

- `online / offline` 是平台可售事实；
- `manual_review` 是自动化处置结果。

因此，不应把 `manual_review` 写入 `listing_status.online_status`，也不应把它描述为商品在平台上的第三种真实上下架状态。

最终建议采用四个相互独立的权威来源，并在其上实时派生自动化门禁：

```text
平台可售事实
online_status

页面位置观察
listing_sync_snapshot / listing_location

人工异常处置
listing_anomaly_cases / review_tasks

实时派生的自动化门禁
decision / allowed_actions / block_reasons_by_action

写操作状态
operation.status / side_effect_state / write_lock
```

## 2. `online_status` 的最终定义

保留当前数据库约束：

```text
online
offline
```

规范语义修改为：

| 值 | 准确定义 |
|---|---|
| `online` | 完整扫描后确认商品当前存在于“上架中”，属于活动销售商品 |
| `offline` | 完整扫描后确认商品当前不在“上架中”，不再属于活动销售商品 |

`offline` 不再表示“已经证明商品唯一存在于待上架”。它只表达商品当前不在线销售。

这样可以继续满足现有价格任务资格规则：

```text
只有 online 商品才可能具有改价资格
offline 商品不能进入正式改价队列
```

但在加入页面冲突和人工介入机制后，价格任务不能只判断 `online_status=online`，还必须确认不存在当前人工介入或活动 UNKNOWN 写锁。

## 3. `not_online` 的定义

`not_online` 不作为数据库枚举，也不作为第三种商品状态。

它只是 SET_OFFLINE 的后置验证条件：

```text
完整刷新并扫描“上架中”
AND 目标商品出现次数为 0
→ postcondition = not_online
→ SET_OFFLINE 完成
→ online_status = offline
```

因此：

- `not_online` 是执行过程中的判断谓词；
- `offline` 是写入数据库的平台可售事实。

两者在下架任务完成、价格任务资格和是否仍在平台销售方面没有实质差异。

## 4. `offline` 与 `waiting_only` 的区别

`offline` 和 `waiting_only` 不能继续作为同义词。

| 概念 | 类型 | 含义 |
|---|---|---|
| `offline` | 平台可售事实 | 商品不在“上架中” |
| `waiting_only` | 页面位置观察 | 商品唯一存在于“待上架”，且不在“上架中” |

下架流程只扫描“上架中”，因此下架完成后可以确认：

```text
online_status = offline
```

但不能据此写入：

```text
listing_location = waiting_only
```

下架流程不需要为了补全页面位置而额外扫描“待上架”。

## 5. `listing_location` 页面观察维度

`listing_location` 只描述一次完整 SYNC_STATUS 的两页观察结果：

```text
online_only
waiting_only
both
neither
ambiguous
```

定义如下：

| “上架中”观察 | “待上架”观察 | `listing_location` |
|---|---|---|
| 唯一存在 | 不存在 | `online_only` |
| 不存在 | 唯一存在 | `waiting_only` |
| 存在 | 存在 | `both` |
| 不存在 | 不存在 | `neither` |
| 任一页面身份重复或映射不唯一 | 任意 | `ambiguous` |

页面扫描没有完成、结束标记无法确认或扫描结果损坏时，不生成新的 `listing_location`，而是将整个 SYNC_STATUS 快照标记为失败。读取失败不能被伪装成商品 `neither` 或人工介入。

`snapshot_complete` 是整次两页扫描的属性，不属于单个商品项。页面观察必须使用“父快照 + 商品项”两层结构，才能证明某商品的 `neither` 确实来自一次完整扫描，而不是某个孤立商品行自行声明完整。

`listing_location` 是具体快照中的证据，不能作为未来写操作跳过新鲜扫描的授权。

## 6. 上下架写操作统一使旧位置快照失效

SET_ONLINE 和 SET_OFFLINE 都会改变商品所在页面，因此必须使用同一条失效规则：

```text
任何上架或下架最终确认被点击
→ 记录 last_listing_change_at
→ 记录 last_listing_operation_id
→ 使此前 listing_location 快照失效
```

失效边界应在最终状态确认按钮被点击时写入，而不是等到业务结果 VERIFIED 后才写入。原因是点击后即使回读失败，平台位置也可能已经改变，旧快照同样不再可靠。

建议在 `listing_status` 增加：

```text
last_listing_change_at
last_listing_operation_id
```

字段名中的 `change` 表示“可能产生上下架变化的最后写入边界”，不单独证明业务操作已经成功。

当前快照只有同时满足以下条件时，才能作为有效位置证据：

```text
snapshot.snapshot_complete = true
AND snapshot.scan_started_at >= listing_status.last_listing_change_at
```

使用 `scan_started_at` 而不是只比较 `scan_completed_at`，避免把“写操作发生前已经开始、写操作后才结束”的跨界扫描误判为有效。

历史快照不删除。价格更新不会改变商品所在页面，因此不会使 `listing_location` 快照失效。

SET_OFFLINE 成功后：

```text
online_status = offline
```

但不得推断：

```text
listing_location = waiting_only
```

SET_ONLINE 最终验证通常只扫描“上架中”。目标出现时可以写入：

```text
online_status = online
```

但不得推断：

```text
listing_location = online_only
```

只有下一次完整两页 SYNC_STATUS 才能生成新的有效 `listing_location`。

## 7. SET_ONLINE 的新鲜同步门禁

任何 SET_ONLINE 写入前都必须完成本轮新的两页 SYNC_STATUS。

无论数据库此前记录的是：

- `online_status=offline`；
- 上一次同步为 `waiting_only`；
- 刚完成下架并确认 `not_online`；

都必须走同一条执行路径：

```text
刷新商品页
→ 完整扫描“上架中”
→ 完整扫描“待上架”
→ 生成本轮 snapshot_id
→ 校验身份和页面位置
→ 本轮确认目标为 waiting_only
→ 才允许修改价格和库存
→ 统一回读
→ 依次执行上架
```

历史 `waiting_only` 不能成为直接上架的授权。本轮新鲜 `waiting_only` 才是 SET_ONLINE 的写入门禁。

## 8. 嵌入式 SYNC_STATUS 与独立 SYNC_STATUS

SET_ONLINE 开头的两页完整扫描本身就是本次请求的嵌入式 SYNC_STATUS：

```text
同一请求
同一登录会话
同一次页面刷新
同一个 snapshot_id
同一份扫描结果直接生成上架计划
```

独立 SYNC_STATUS 用于日常全量盘点；嵌入式 SYNC_STATUS 用于本次 SET_ONLINE 写入门禁。两者复用同一套扫描函数、映射规则、结束判定和完整性检查。

禁止为了满足门禁执行重复扫描：

```text
先独立扫描两页
→ 随后 SET_ONLINE 再次扫描相同两页
```

只有第一次快照已失效、页面被主动刷新或发生了明确的外部状态变化时，才重新扫描。

## 9. 自动化处置必须实时派生

`automation_disposition` 不应成为需要独立维护的长期状态源，否则可能出现页面位置已经恢复正常，但旧的 `manual_review` 值仍残留的问题。

API 和运营页面不应只输出一个笼统的可操作布尔值，而应按动作类型返回门禁决策：

```text
decision = EXECUTE / ALREADY_APPLIED / BLOCKED
allowed_actions
block_reasons_by_action
```

派生输入至少包括：

```text
最新有效 listing_location
open review_tasks
ACTIVE/UNKNOWN/REVIEW_BLOCKED 商品写锁
当前动作是否要求本轮新鲜快照
```

基础动作矩阵：

| 当前事实 | 动作 | 门禁决策 |
|---|---|---|
| 有效 `online_only` | `UPDATE_PRICE` | `EXECUTE` |
| 有效 `online_only` | `SET_OFFLINE` | `EXECUTE` |
| 有效 `waiting_only` | `SET_ONLINE` | `EXECUTE` |
| 完整“上架中”扫描确认目标出现次数为 0 | `SET_OFFLINE` | `ALREADY_APPLIED`，不得重复点击 |
| 有效 `online_only`，价格和库存均符合目标 | `SET_ONLINE` | `ALREADY_APPLIED` |
| 有效 `online_only`，价格或库存不符合目标 | `SET_ONLINE` | `BLOCKED`，创建 Review，不自动修改资料 |
| `both/neither/ambiguous` | 任意写操作 | `BLOCKED` |
| 存在 ACTIVE/UNKNOWN/REVIEW_BLOCKED 写锁 | 任意写操作 | `BLOCKED` |

统一门禁函数建议为：

```text
evaluate_automation_gate(
    action_type,
    internal_sku,
    current_snapshot_id
)
```

阻断原因至少包括：

```text
LISTING_ANOMALY_REVIEW_OPEN
OPERATION_RECONCILIATION_PENDING
PARTIAL_OPERATION_REVIEW_PENDING
FRESH_SYNC_REQUIRED
EXPECTED_ONLINE_ONLY
EXPECTED_WAITING_ONLY
```

运营页面可以把 `LISTING_ANOMALY_REVIEW_OPEN` 显示为“待人工介入”，但不能把 `OPERATION_RECONCILIATION_PENDING` 也简化为普通人工通知。

## 10. `manual_review` 的细分和关闭规则

不增加第二套人工复核生命周期。项目已有 `review_tasks`、Web Review、Mobile Review、Review Token、通知 Outbox 和审计记录，任务13必须继续以 `review_tasks` 作为人工处置的唯一权威。

页面异常事实单独保存为 `listing_anomaly_cases`，并通过 `review_task_id` 关联现有 Review：

```text
listing_anomaly_cases
    保存异常事实、快照来源和清除证据

review_tasks
    保存人工处理状态、处理人和解决结果
```

建议增加 Review 类型：

```text
listing_location_anomaly
listing_data_mismatch
shadowbot_partial_operation
```

任务13第一版不新增全局 `ReviewTaskStatus.RESOLVED`，继续复用现有 `cancelled`。页面异常被新快照自动清除时，表示该人工处理请求已不再需要，必须在 `resolution_payload_json` 中写入：

```json
{
  "resolution_type": "AUTO_CLEARED_BY_SNAPSHOT",
  "resolved_by_snapshot_id": "SNAPSHOT-...",
  "previous_reason_code": "PRESENT_IN_BOTH_LISTS"
}
```

同时填写：

```text
resolved_by = system:listing_sync
resolved_at
resolution_note
```

该规则只适用于系统生成且未被人工锁定的 `listing_location_anomaly`。`shadowbot_partial_operation` 不得自动取消。

原因代码：

```text
PRESENT_IN_BOTH_LISTS
ABSENT_FROM_BOTH_LISTS
DUPLICATE_PAGE_IDENTITY
UNMAPPED_PRODUCT
IDENTITY_MAPPING_CONFLICT
```

由于当前 SYNC_STATUS 只扫描“上架中”和“待上架”，系统不能可靠区分审核中、未通过和商品未录入。这些情况统一表现为：

```text
listing_location = neither
reason_code = ABSENT_FROM_BOTH_LISTS
review_type = listing_location_anomaly
```

如果人工后续获得具体原因，可以写入 `diagnostic_message`，但不新增平台状态或自动化分支。

### 10.1 Review 对动作的阻断矩阵

不同 Review 类型可以阻断不同动作，但不能由各业务入口自行猜测。Review payload 必须保存规范化的：

```json
{
  "blocked_actions": [
    "update_price",
    "set_online",
    "set_offline"
  ],
  "reason_code": "LISTING_DATA_MISMATCH",
  "resolution_policy": "MANUAL_ONLY"
}
```

默认矩阵：

| Review 类型 | `UPDATE_PRICE` | `SET_ONLINE` | `SET_OFFLINE` |
|---|---|---|---|
| `listing_location_anomaly` | 阻断 | 阻断 | 阻断 |
| `shadowbot_partial_operation` | 阻断 | 阻断 | 阻断 |
| `listing_data_mismatch` | 按原因决定；第一版默认阻断 | 阻断 | 按原因决定；第一版默认阻断 |

多个 open Review 同时存在时，对 `blocked_actions` 取并集。字段缺失、非法或包含未知动作时采用 fail-closed，默认阻断全部写操作。

统一门禁函数读取经过校验的 `blocked_actions`，业务入口不得直接解释 `review_type`。后续如需缩小 `listing_data_mismatch` 的阻断范围，必须先冻结对应 `reason_code` 策略并补充测试。

为避免过期 Review 长期阻断自动化，需要明确关闭规则：

- 由系统自动创建、尚未被人工锁定的页面异常 Review，可以由后续完整、正常的新快照自动解决；
- 异常事实表必须记录 `cleared_by_snapshot_id` 和 `cleared_at`，对应的 `review_tasks` 在同一事务中更新；
- 人工明确锁定或要求人工确认的 Review 不自动关闭；
- `shadowbot_partial_operation` 使用 `MANUAL_ONLY` 解决策略，不允许正常 SYNC_STATUS 自动关闭；
- UNKNOWN operation 的写锁不能由 Review 自动关闭逻辑释放。

## 11. SYNC_STATUS 失败不等于人工介入

以下情况属于同步运行失败，不属于商品人工介入：

- 任一页面扫描提前终止；
- 页面结束标记无法确认；
- 页面刷新失败；
- 结果文件或 phase 写入失败；
- Worker 中途停止；
- 整体快照无法证明完整。

正确处理为：

```text
SYNC_STATUS 失败
→ 快照主表记录失败
→ 不投影商品 online_status
→ 不生成新的有效 listing_location
→ 不批量创建 listing_anomaly_cases 或 review_tasks
→ 保留失败证据并按运行错误处理
```

只有在完整、有效的两页快照中得到 `both / neither / ambiguous`，才生成商品人工介入记录。

## 12. `manual_review` 与 operation UNKNOWN 必须分离

页面位置异常由 `listing_location`、`listing_anomaly_cases` 和关联的 `review_tasks` 表达；写操作副作用无法确认则必须进入：

```text
operation.status = NEEDS_RECONCILIATION
side_effect_state = UNKNOWN
write_lock = 保持占用或 UNKNOWN
```

点击最终确认后回读失败时，不能只发送普通人工通知并结束。必须使旧位置快照失效、保留写锁，并进入该 operation 的唯一 RECONCILE。

同一商品可以同时具有未解决的 `review_tasks` 和 `NEEDS_RECONCILIATION` operation。Review 不能替代 operation 对账，也不能自动释放写锁。

### 12.1 SET_ONLINE RECONCILE 输入

SET_ONLINE 是“保存价格/库存 + 完成上架”的复合写操作。operation 必须保存：

```text
observed_price_before_action
observed_inventory_before_action
target_price
target_inventory
price_change_required
inventory_change_required
observed_price_after_detail_save
observed_inventory_after_detail_save
```

RECONCILE 必须完整扫描“上架中”和“待上架”，并同时核对页面位置、价格和库存。只确认商品在线不足以证明整个上架任务成功。

未要求变化的字段必须记录为 `NOT_REQUIRED`，不参与“是否部分生效”的判断。否则当操作前值原本就等于目标值时，会同时满足“等于操作前值”和“等于目标值”，导致 NOT_APPLIED 与 PARTIALLY_APPLIED 条件重叠。

### 12.2 SET_ONLINE RECONCILE 结果矩阵

以下矩阵仅在两页扫描完整、目标身份唯一且价格库存可可靠读取时适用：

| 页面位置 | 价格和库存 | operation 结果 | 写锁处理 |
|---|---|---|---|
| `online_only` | 均等于目标值 | `VERIFIED` | 释放 |
| `waiting_only` | 均等于操作前值 | `NOT_APPLIED` | 释放 |
| `waiting_only` | 至少一个原本需要改变的字段已等于目标值 | `PARTIALLY_APPLIED`：资料已保存但未上线 | `REVIEW_BLOCKED` |
| `waiting_only` | 出现既不等于操作前值、也不等于目标值的已确认值 | `PARTIALLY_APPLIED` | `REVIEW_BLOCKED` |
| `online_only` | 价格或库存不符合目标 | `PARTIALLY_APPLIED`：已上线但资料不符合合同 | `REVIEW_BLOCKED` |
| `both/neither/ambiguous` | 任意 | 继续 `NEEDS_RECONCILIATION/UNKNOWN` | 保持 |
| 扫描不完整或关键值无法读取 | 无法判断 | 继续 `NEEDS_RECONCILIATION/UNKNOWN` | 保持 |

`NOT_APPLIED` 只有在商品仍为 `waiting_only`，且所有原本需要改变的字段都仍等于操作前值时才能使用。仅知道“不等于目标值”不能证明完全未生效。

Runtime Schema v13 应新增明确的 `PARTIALLY_APPLIED` operation 状态和 `REVIEW_BLOCKED` 写锁状态。部分副作用已经可判定，因此不再使用 UNKNOWN 锁；但也不能释放为 `RELEASED`，否则安全性将完全依赖每个写入入口都正确检查 Review。

锁状态规则冻结为：

| operation 结果 | 写锁状态 |
|---|---|
| `VERIFIED` | `RELEASED` |
| `NOT_APPLIED` | `RELEASED` |
| `PARTIALLY_APPLIED` | `REVIEW_BLOCKED` |
| `NEEDS_RECONCILIATION` | `UNKNOWN` |

`shadowbot_partial_operation` Review 的人工解决与 `REVIEW_BLOCKED` 释放必须在同一数据库事务中完成。仅仅创建一条新任务不能自动解除该阻断。

### 12.3 两阶段副作用状态

SET_ONLINE 不能只依赖一个总体 `side_effect_state`。逐商品结果至少需要：

```text
detail_effect_state
listing_effect_state
operation_status
```

资料阶段建议枚举：

```text
NOT_REQUIRED
NOT_STARTED
INTENT_RECORDED
CLICKED
VERIFIED
PARTIAL
UNKNOWN
```

上架阶段建议枚举：

```text
NOT_STARTED
INTENT_RECORDED
CLICKED
VERIFIED
NOT_APPLIED
UNKNOWN
```

总体 `operation_status` 由两个子状态推导。阶段状态和 checkpoint 共同构成审计证据，不能只保存一个总体 `PARTIALLY_APPLIED`。

### 12.4 PARTIALLY_APPLIED Review 的解决动作

人工 Review 必须冻结以下三种处理：

| 人工处理结果 | operation 处理 | 写锁处理 |
|---|---|---|
| 确认当前平台结果可接受，人工处理完成 | 生命周期转为 `MANUAL_HANDLED`，保留原 `PARTIALLY_APPLIED` 结果证据 | 同一事务释放 |
| 需要继续执行纠正操作 | 原 operation 保留 `PARTIALLY_APPLIED`，记录 `resolution_status=CORRECTIVE_ACTION_AUTHORIZED` 和 `superseded_by_operation_id` | 在同一事务转移给新 operation |
| 暂时无法处理 | 保持 `PARTIALLY_APPLIED` | 保持 `REVIEW_BLOCKED` |

原始部分生效结果不能因为后续纠正而被覆盖或丢失。建议将执行结果和人工解决状态分别保存：

```text
operation_result = PARTIALLY_APPLIED
resolution_status = UNRESOLVED / MANUAL_HANDLED / CORRECTIVE_ACTION_AUTHORIZED
resolved_by
resolved_at
superseded_by_operation_id
```

禁止以下流程：

```text
Review 选择“需要纠正”
→ 先释放 REVIEW_BLOCKED
→ 稍后创建新任务或新 operation
```

推荐纠正事务：

```text
人工批准纠正方案
→ 创建明确授权的新任务或授权记录
→ 创建新 operation
→ 将 REVIEW_BLOCKED 锁从旧 operation 原子转移给新 operation
→ 新 operation 获得 ACTIVE 锁
→ 记录旧 operation 的 resolution_status 和 superseded_by_operation_id
→ 更新 review_tasks 并取消旧通知
→ 提交
```

任务13第一版可以只实现“人工接受并释放”和“继续保持阻断”，将自动创建纠正 operation 延期；但普通新任务在任何情况下都不能绕过或释放 `REVIEW_BLOCKED`。

### 12.5 SET_ONLINE 两阶段批次中断矩阵

SET_ONLINE 先保存全部商品资料并统一回读，再依次执行上架。因此“尚未点击上架”不等于“未产生副作用”。

#### 资料保存阶段中断

| 商品位置 | 已知事实 | batch item | operation | effect state | 写锁 |
|---|---|---|---|---|---|
| 已成功保存并回读的前序商品 | 资料已变更，尚未上架 | `PARTIALLY_APPLIED` | `PARTIALLY_APPLIED` | detail=`VERIFIED`，listing=`NOT_STARTED` | `REVIEW_BLOCKED` |
| 当前商品保存点击后无法确认 | 资料副作用不确定 | `UNKNOWN` | `NEEDS_RECONCILIATION` | detail=`UNKNOWN` | `UNKNOWN` |
| 尚未开始保存的后续商品 | 无副作用 | `NOT_ATTEMPTED` | `NOT_APPLIED` | detail/listing=`NOT_STARTED` | `RELEASED` |

operation 层不新增 `NOT_ATTEMPTED`。尚未开始保存的项目使用：

```text
batch_item.status = NOT_ATTEMPTED
operation.status = NOT_APPLIED
side_effect_state = NOT_STARTED
error_code = BATCH_ABORTED_BEFORE_DETAIL_SAVE
```

#### 统一资料回读失败

必须逐商品分类：

- 已可靠确认所有要求变化的资料等于目标值：`PARTIALLY_APPLIED / REVIEW_BLOCKED`；
- 明确所有要求变化的资料仍等于操作前值：根据该商品实际保存阶段归类；
- 无法确认资料是否保存：`NEEDS_RECONCILIATION / UNKNOWN`；
- 本批次不得开始任何正式上架确认。

不能因为一个商品回读失败而把所有商品统一标记为 UNKNOWN。

#### 正式上架阶段中断

| 商品位置 | 已知事实 | operation | 写锁 |
|---|---|---|---|
| 已完成最终上架和完整回读的前序商品 | 资料和上架均已生效 | `VERIFIED` | `RELEASED` |
| 当前商品最终确认后无法回读 | 上架副作用不确定 | `NEEDS_RECONCILIATION` | `UNKNOWN` |
| 后续商品资料已保存，但尚未点击上架 | 已发生资料副作用 | `PARTIALLY_APPLIED` | `REVIEW_BLOCKED` |

最后一类不得记录为 `NOT_ATTEMPTED`。它只能表示“上架动作未尝试”，不能否定此前已经发生的资料保存副作用。

批次存在 UNKNOWN 时继续使用：

```text
batch_status = UNKNOWN
```

同时必须准确保存并验证：

```text
verified_count
unknown_count
partial_effect_count
not_attempted_count
failed_count

verified_count
+ unknown_count
+ partial_effect_count
+ not_attempted_count
+ failed_count
= batch_target_count
```

Result Importer、Watchdog 恢复、Markdown 报告和 CI 都必须使用同一套中断矩阵和计数恒等式。

## 13. 下架流程的最终语义

SET_OFFLINE 的正式流程保持：

```text
登录和 Worker 状态检查
→ 刷新并完整扫描“上架中”
→ 确认全部目标唯一存在并记录排序
→ 记录每个目标的下架前价格
→ 按页面顺序依次下架
→ 每次最终确认点击时使旧位置快照失效
→ 刷新并重新完整扫描“上架中”
→ 确认全部目标出现次数为 0
→ online_status = offline
→ 保存下架前价格
→ 输出结果
```

不扫描“待上架”，也不根据本次下架结果推断 `listing_location=waiting_only`。

## 14. 上架流程的最终语义

SET_ONLINE 的正式流程保持：

```text
登录和 Worker 状态检查
→ 刷新商品管理页
→ 执行嵌入式 SYNC_STATUS
→ 完整扫描“上架中”和“待上架”
→ 对 online_only 目标执行幂等检查
→ 对 waiting_only 目标生成写入计划
→ both / neither / ambiguous 在首次写入前阻断整批
→ 记录操作前价格和库存
→ 计算需要修改的价格和库存字段
→ 修改并保存全部目标
→ 统一回读价格和库存
→ 全部回读一致后依次上架
→ 每次最终确认点击时使旧位置快照失效
→ 刷新并扫描“上架中”
→ 唯一存在且价格库存符合目标的项目判定成功
→ 成功项写入价格、库存和 online 状态
→ 不推断 listing_location = online_only
→ 输出结果
```

如果最终确认点击后无法完成可靠回读，必须进入 operation UNKNOWN 和唯一 RECONCILE，不能直接归入普通人工介入。

SET_ONLINE 的幂等规则：

| 当前页面事实 | 价格和库存 | 处理 |
|---|---|---|
| `online_only` | 均符合目标 | `ALREADY_APPLIED`，不得修改或重复上架 |
| `online_only` | 任一不符合目标 | 阻断并创建 `listing_data_mismatch` Review；任务13第一版不自动扩展为资料修改任务 |
| `waiting_only` | 任意 | 进入正常 SET_ONLINE 计划 |

全目标门禁必须在首次资料保存之前完成。任一目标出现以下情况时整批 `BLOCKED`：

```text
both / neither / ambiguous
online_only 但价格或库存不符合任务目标
商品身份不唯一
价格、库存或其他关键字段无法读取
存在 open Review
存在 ACTIVE / UNKNOWN / REVIEW_BLOCKED 写锁
```

整批阻断必须保证：

```text
0 次资料保存
0 次最终上架确认
```

`ALREADY_APPLIED` 项可以与合法 `waiting_only` 项共存；异常项不能与可执行项共存后继续写入。例如商品 A 已在线但资料不符合目标时，即使商品 B 为可正常上架的 `waiting_only`，整个批次也必须在首次写入前停止。

SET_OFFLINE 在完整“上架中”预扫描中确认目标出现次数为 0 时，同样返回 `ALREADY_APPLIED`，不得再次点击下架。

## 15. `listing_location` 到 `online_status` 的投影矩阵

只有完整、有效的两页 SYNC_STATUS 快照才能执行投影：

| `listing_location` | `online_status` 投影 | 自动化处理 |
|---|---|---|
| `online_only` | 更新为 `online` | 无异常 Review |
| `waiting_only` | 更新为 `offline` | 无异常 Review |
| `both` | 更新为 `online` | 创建 Review，禁止自动写入 |
| `neither` | 更新为 `offline` | 创建 Review，禁止自动写入 |
| `ambiguous` | 保留原值，不覆盖 | 创建 Review，禁止自动写入 |

`both` 仍投影为 `online`，因为商品确实存在于“上架中”，仍可能在平台销售。冲突事实由 `listing_location=both` 和 Review 表达。

`ambiguous` 不覆盖 `online_status`，因为无法证明页面观察属于哪个内部 SKU。

## 16. 价格任务资格分阶段调整

不能在任务13初期立刻要求所有改价任务都具有新鲜完整的两页快照，否则可能阻断任务12已经验收的能力。

### 16.1 第一阶段立即启用

```text
online_status = online
AND 无 open listing anomaly review
AND 无该 SKU 的 ACTIVE / UNKNOWN / REVIEW_BLOCKED 写锁
```

价格任务生成和发布不得重复手写锁枚举，应统一调用：

```text
evaluate_automation_gate(
    action_type = UPDATE_PRICE,
    internal_sku = ...
)
```

任务12继续保留自己的实时“上架中”页面预扫描、动态唯一定位和旧价门禁，不依赖历史位置快照获得写入授权。任务13第一阶段的 UPDATE_PRICE 门禁不强制要求新鲜两页快照，避免回退已验收能力。

### 16.2 SYNC_STATUS 稳定后启用

追加以下硬门禁：

```text
如果存在最新有效 listing_location
则不得为 both / neither / ambiguous
```

建议先以观察模式记录“如果启用该规则将阻断哪些任务”，确认同步覆盖率后再改为硬阻断。

快照 TTL 暂不硬编码进任务13第一版。后续可以通过平台策略配置，但不能替代任务12执行时的实时页面预扫描。

## 17. Runtime Schema v13 数据库建议

不修改 `listing_status.online_status` 的 `online/offline` 枚举约束。

`listing_status` 增加：

```text
last_listing_change_at
last_listing_operation_id
online_status_observed_at
online_status_source_type
online_status_source_id
```

`online_status_source_type` 至少支持：

```text
SYNC_STATUS
SET_ONLINE_POSTCHECK
SET_OFFLINE_POSTCHECK
RECONCILE
```

价格更新只更新时间和价格来源，不得让 `online_status` 看起来也被重新确认。

页面快照采用父表：

```text
listing_sync_snapshots
  snapshot_id
  platform_name
  execution_attempt_id
  scan_started_at
  scan_completed_at
  online_scan_started_at
  online_scan_completed_at
  waiting_scan_started_at
  waiting_scan_completed_at
  online_scan_complete
  waiting_scan_complete
  online_end_marker_verified
  waiting_end_marker_verified
  snapshot_complete
  instruction_hash
  result_id
  status
  error_code
  evidence_manifest_sha256
```

商品项采用子表：

```text
listing_sync_snapshot_items
  snapshot_item_id
  snapshot_id
  internal_sku
  product_name
  grade
  page_identity_key
  online_occurrences
  waiting_occurrences
  listing_location
  online_row_identities
  waiting_row_identities
  online_observed_price
  waiting_observed_price
  online_observed_inventory
  waiting_observed_inventory
  diagnostic_code
  online_observed_at
  waiting_observed_at
```

`internal_sku` 允许为空，以便保留未映射页面商品。价格、库存和观察时间按页面分别保存，避免 `both` 场景下单个值无法表达页面差异。商品不在某页时不存在商品行级观察时间，其缺失证据来自父快照对应页面的完成时间和结束标记。SYNC_STATUS 中的库存仅作为证据，不投影到正式库存。

页面异常事实表：

```text
listing_anomaly_cases
  anomaly_case_id
  snapshot_id
  snapshot_item_id
  platform_name
  internal_sku NULL
  page_identity_key
  affected_internal_skus_json
  anomaly_subject_key
  dedupe_key
  reason_code
  diagnostic_message
  resolution_policy
  created_at
  cleared_at
  cleared_by_snapshot_id
  review_task_id
```

`listing_anomaly_cases` 只保存异常事实，不维护第二套 Review 状态；人工生命周期继续引用现有 `review_tasks`。建议增加：

```text
review_type = listing_location_anomaly
review_type = listing_data_mismatch
review_type = shadowbot_partial_operation
```

其中 `shadowbot_partial_operation` 使用 `MANUAL_ONLY` 解决策略。

异常主体规则：

| `reason_code` | 主体和映射 |
|---|---|
| `UNMAPPED_PRODUCT` | `internal_sku=NULL`，使用 `platform_name + page_identity_key` |
| `IDENTITY_MAPPING_CONFLICT` | 保存排序规范化后的全部 `affected_internal_skus_json` |
| `ABSENT_FROM_BOTH_LISTS` | 使用明确的 `platform_name + internal_sku` |
| `DUPLICATE_PAGE_IDENTITY` | 同时绑定 `snapshot_item_id` 和 `page_identity_key` |

`anomaly_subject_key` 和 `dedupe_key` 必须由规范化主体生成。`affected_internal_skus_json` 写入前按 SKU 排序，避免相同冲突因为输入顺序不同而创建重复 Review。

### 17.1 通用 operation

当前 `shadowbot_operations` 强制要求价格字段，无法直接承载纯下架操作。v13 必须泛化为：

```text
action_type
expected_old_price NULL
target_price NULL
expected_old_status
target_status
target_inventory
approved_payload_json
```

应用层按动作类型执行条件必填校验：

- v4 `UPDATE_PRICE` 继续强制要求旧价和目标价；
- `SET_ONLINE` 强制要求旧状态、目标状态、目标价格和目标库存；
- `SET_OFFLINE` 强制要求旧状态和目标状态，不要求虚假价格或库存。

SET_ONLINE operation 详情还需增加：

```text
observed_price_before_action
observed_inventory_before_action
price_change_required
inventory_change_required
observed_price_after_detail_save
observed_inventory_after_detail_save
target_inventory
detail_effect_state
listing_effect_state
```

operation 和 side-effect 状态增加 `PARTIALLY_APPLIED`。

### 17.2 通用批次身份

当前共享写锁的 `batch_id` 外键直接引用 v4 `shadowbot_commit_batches`。如果 v5 上下架使用新批次表，写锁无法直接共享批次身份。

建议新增只负责公共身份的：

```text
shadowbot_batch_registry
  batch_id
  batch_type
  contract_version
  platform_name
  created_at
```

v4 改价批次和 v5 上下架批次分别一对一引用 registry；共享写锁也引用 registry。registry 不维护另一套批次业务状态，具体状态继续由 v4/v5 子表负责。

### 17.3 写锁状态

`shadowbot_write_locks.status` 从：

```text
ACTIVE / UNKNOWN / RELEASED
```

扩展为：

```text
ACTIVE / UNKNOWN / REVIEW_BLOCKED / RELEASED
```

所有写入入口、锁冲突查询、锁获取 UPSERT 和 Web 展示都必须把 `REVIEW_BLOCKED` 视为不可写状态。Review 解决和锁释放必须使用同一数据库事务。

### 17.4 完整快照的原子导入

完整 SYNC_STATUS 成功后的数据库处理必须在一个 SQLite `BEGIN IMMEDIATE` 事务内完成：

```text
接受并验证结果回执
→ 写入 listing_sync_snapshots
→ 写入 listing_sync_snapshot_items
→ 投影 online_status
→ 创建、更新或清除 listing_anomaly_cases
→ 创建或自动取消 review_tasks
→ 创建或取消 notification_outbox
→ 提交
```

任何一步失败时整体回滚，不得留下以下部分状态：

```text
online_status 已更新，但异常 Review 未创建
Review 已取消，但 anomaly 尚未清除
异常已创建，但初始通知 Outbox 未持久化
```

应复用现有 Review 与通知 Outbox 的原子业务模式。文件 ACK 只能在数据库事务成功提交后写出；SQLite 回滚时不得向 Worker 表示结果已经成功导入。

### 17.5 任务11/12库存行为边界

“SYNC_STATUS 中库存只作为证据、不投影正式库存”仅约束任务13的两页状态同步，不修改任务11/任务12既有 READ_ONLY 对价格、平台库存、上下架状态和观察时间的投影行为。

任务13成功 SET_ONLINE 后，只有同时满足以下条件才能更新现有库存字段：

```text
目标库存已保存
AND 保存后独立回读一致
AND 最终确认商品已经上架成功
```

投影使用现有字段：

```text
platform_stock_qty = 独立回读库存
inventory_source = SET_ONLINE_POSTCHECK
inventory_observed_at = 回读时间
inventory_source_attempt_id = 本次 execution_attempt_id
```

SET_OFFLINE、任务13 SYNC_STATUS、UNKNOWN、PARTIALLY_APPLIED 和未完成上架的结果均不得覆盖正式库存。

### 17.6 v12 到 v13 迁移门禁

v13 必须无损迁移任务12现有数据。数据库序列化继续使用与 `TaskActionType` 一致的小写规范值：

```text
action_type = update_price
batch_type = update_price
```

大写 `UPDATE_PRICE` 只作为文档中的枚举标签。

现有 v4 operation 回填：

```text
action_type = update_price
expected_old_status = NULL
target_status = NULL
target_inventory = NULL
```

现有 operation ID、task ID、价格、状态、approved payload hash、attempt、checkpoint 和证据引用必须保持不变。

公共批次注册表回填：

```text
对每个现有 shadowbot_commit_batches：
  shadowbot_batch_registry.batch_id = 原 batch_id
  batch_type = update_price
  contract_version = 4
  platform_name = 原 platform_name
  created_at = 原 created_at
```

迁移顺序：

```text
BEGIN IMMEDIATE
→ 创建 shadowbot_batch_registry
→ 回填全部 v4 batch registry
→ 泛化并回填 shadowbot_operations
→ 重建 shadowbot_write_locks 外键
→ 重建索引和唯一约束
→ PRAGMA foreign_key_check
→ PRAGMA integrity_check
→ runtime schema health
→ COMMIT
```

写锁迁移必须逐行保留已有 `ACTIVE / UNKNOWN / RELEASED`，不得把历史 UNKNOWN 锁误转为 RELEASED。迁移前后至少验证：

- operation、attempt、checkpoint、batch、item、receipt、lock 行数一致；
- 所有业务 ID、instruction hash 和 manifest hash 不变；
- 写锁状态逐行一致；
- 表级规范化摘要或哈希一致；
- 无孤儿外键。

迁移测试至少覆盖：

```text
v12 有数据数据库升级至 v13
重复执行迁移幂等
v4 VERIFIED 批次仍可查询
v4 UNKNOWN / RECONCILE 账本完整
历史写锁状态不变
任务12现有测试和证据验证继续通过
v13 新库初始化和 schema health 通过
备份恢复后无孤儿外键
```

这些测试是“任务13增强但不破坏任务12”的发布门禁。

### 17.7 phase、Importer 与点击边界

ShadowBot Worker 不直接写 SQLite。上下架位置变更边界先写入耐久 phase，再由 Result Importer 或 Watchdog recovery 投影数据库。

```text
最终确认点击前
→ phase = ACTION_INTENT_RECORDED

最终确认点击后
→ phase = ACTION_CLICKED
→ 保存 operation_id
→ 保存 internal_sku
→ 保存 clicked_at
→ 保存 instruction_hash
→ 保存 item_payload_sha256
```

Importer 必须校验 phase 与请求、operation、商品项和 hash 的绑定，然后投影：

```text
listing_status.last_listing_change_at = clicked_at
listing_status.last_listing_operation_id = operation_id
```

Watchdog 在 result 缺失时同样读取耐久 phase：

- 存在有效 `ACTION_CLICKED`：旧位置快照失效，operation 转为需要对账；
- 只有 intent、没有 clicked：不得假设已经改变平台位置；
- phase 缺失、损坏或 hash 不一致：隔离结果，不能猜测。

在结果尚未导入期间：

```text
ACTIVE / UNKNOWN 写锁阻止新的写操作
旧位置快照不得作为新写操作授权
phase 是点击边界的耐久事实
SQLite 是最终投影事实
```

正常队列继续遵守“上一结果完成导入并写 ACK 后才能发布下一请求”。后续 SYNC_STATUS 不得越过尚未导入的上下架写操作进行平台状态投影。

文件 ACK 只能在相关数据库事务成功提交后生成。

## 18. operation、Task 和 Batch 投影

任务13必须冻结 operation 结果如何投影到任务、锁和运营展示：

| operation 结果 | Task 状态 | 写锁状态 | 后续处理 |
|---|---|---|---|
| `VERIFIED` | `success` | `RELEASED` | 正常完成 |
| `NOT_APPLIED` | `failed` | `RELEASED` | 不自动恢复 pending；需要新的任务重新授权 |
| `PARTIALLY_APPLIED` | `manual_review` | `REVIEW_BLOCKED` | 创建 `shadowbot_partial_operation` Review |
| `NEEDS_RECONCILIATION` | `manual_review`，附 `reconciliation_pending=true` | `UNKNOWN` | 只允许唯一 RECONCILE |

`NOT_APPLIED` 不自动恢复为 `pending`，避免被调度器重新投递。后续重试必须来自新的有效任务或 Review 明确授权。

`ALREADY_APPLIED` 在逐商品结果中保留：

```text
action_attempted = false
idempotent_result = ALREADY_APPLIED
```

进入共享 operation 账本后归并为 `VERIFIED`，避免建立第二套终态。

批次状态按以下优先级推导：

```text
存在 NEEDS_RECONCILIATION / UNKNOWN
→ batch_status = UNKNOWN

无 UNKNOWN，但存在 PARTIALLY_APPLIED
→ batch_status = PARTIAL
→ requires_manual_review = true
→ partial_effect_count > 0

无以上状态，成功和明确失败混合
→ batch_status = PARTIAL

全部 VERIFIED 或 ALREADY_APPLIED
→ batch_status = VERIFIED

全部明确失败
→ batch_status = FAILED
```

任务13第一版不必新增 `PARTIAL_REVIEW` 批次枚举，但 Web 和报告必须展示 `requires_manual_review` 和 `partial_effect_count`，不能把已知部分副作用显示为普通 PARTIAL。

## 19. 对原修改意见的具体修正

原修改意见中的以下表达应删除或替换：

1. 删除“商品平台状态为 `online / offline / manual_review`”。
2. 删除“扩展 `listing_status.online_status` 约束以允许 `manual_review`”。
3. 删除“历史 `waiting_only` 可以直接自动上架”。
4. 删除将下架成功等同于“唯一进入待上架”的要求。
5. 删除将点击后回读失败直接归入普通 `manual_review` 的表述。
6. 将 `not_online` 定义为 SET_OFFLINE 后置条件，不作为持久化状态。
7. 将 `waiting_only` 定义为 SYNC_STATUS 页面位置证据，不作为 `offline` 的定义。
8. 明确 SET_ONLINE 开头的两页扫描就是本次嵌入式 SYNC_STATUS，不重复执行相同扫描。
9. 明确所有上架、下架最终确认点击都会使旧位置快照失效。
10. 删除持久化 `automation_disposition`，改为按 `action_type` 实时计算 `EXECUTE / ALREADY_APPLIED / BLOCKED`。
11. 使用父快照和商品项两表，`snapshot_complete` 只保存在父快照。
12. 增加 SET_ONLINE 复合写操作的字段变化计划、两阶段 effect state、`PARTIALLY_APPLIED` 和完整 RECONCILE 矩阵。
13. 明确商品人工介入记录和 operation UNKNOWN 可以同时存在，但不能互相替代。
14. 分阶段增强价格任务资格，不回退任务12的实时页面门禁。
15. 删除第二套 `listing_review_cases` 生命周期，改为 `listing_anomaly_cases + review_tasks`。
16. `PARTIALLY_APPLIED` 使用 `REVIEW_BLOCKED`，不得直接释放写锁。
17. 增加 `online_status` 的观察时间、来源类型和来源 ID。
18. 分别记录“上架中”和“待上架”的扫描时间及商品观察时间。
19. 泛化 operation，并通过 `shadowbot_batch_registry` 解除共享写锁对 v4 批次表的直接依赖。
20. 冻结 operation、Task、Batch 和写锁的投影矩阵。
21. 第一阶段 UPDATE_PRICE 门禁必须包含 `REVIEW_BLOCKED`，并统一调用动作门禁函数。
22. 冻结 PARTIALLY_APPLIED 的人工接受、继续阻断和无空窗纠正锁转移。
23. 任一 SET_ONLINE 异常项在首次写入前阻断整个批次。
24. 异常事实支持无 SKU、多 SKU 冲突和稳定 dedupe key。
25. 快照、投影、异常、Review 和通知 Outbox 必须原子导入。
26. 任务13第一版复用 `ReviewTaskStatus.CANCELLED`，不新增全局 `resolved`。
27. 明确任务13 SYNC_STATUS 库存规则不修改任务11/12既有 READ_ONLY 行为。
28. 冻结 SET_ONLINE 资料保存、统一回读和正式上架三个中断点的逐商品结果矩阵。
29. Review payload 保存经过校验的 `blocked_actions`，多个 open Review 取阻断动作并集。
30. 增加 v12→v13 无损迁移、幂等迁移、表级摘要和任务12回归门禁。
31. 明确 Worker 只写耐久 phase，由 Importer/Watchdog 投影点击边界和快照失效时间。

## 20. 最终关系摘要

```text
online_status
    表达是否仍在平台活动销售
    online / offline

listing_sync_snapshot / listing_location
    表达一次完整两页同步观察到的位置
    online_only / waiting_only / both / neither / ambiguous

listing_anomaly_cases
    表达页面异常事实和快照来源

review_tasks
    作为页面异常和部分写入人工处理的唯一生命周期

automation gate
    按动作实时输出 EXECUTE / ALREADY_APPLIED / BLOCKED
    同时输出 allowed_actions 和 block_reasons_by_action
    不是新的长期事实源

operation
    表达某一次写操作是否已确认产生副作用
    VERIFIED / NOT_APPLIED / PARTIALLY_APPLIED / NEEDS_RECONCILIATION
    PARTIALLY_APPLIED 另有独立 resolution_status

write_lock
    ACTIVE / UNKNOWN / REVIEW_BLOCKED / RELEASED

shadowbot_batch_registry
    只提供 v4/v5 批次和共享写锁的公共身份
    不维护第二套批次业务状态

snapshot importer transaction
    原子写入快照、投影、异常、Review 和通知 Outbox
    数据库提交后才能写文件 ACK

phase / importer boundary
    Worker 写 ACTION_INTENT_RECORDED / ACTION_CLICKED
    Importer 或 Watchdog 投影 last_listing_change_at

v12 → v13 migration
    无损回填 update_price operation 和 v4 batch registry
    保留历史 UNKNOWN 写锁及任务12全部账本
```

该模型能够同时满足：

- 现有 `online/offline` 数据约束；
- 只有在线商品可改价的资格规则；
- 下架后不扫描待上架的效率要求；
- 上架前强制执行新鲜 SYNC_STATUS；
- 所有上下架写操作统一使旧位置快照失效；
- 页面冲突和商品缺失的人工介入；
- SET_ONLINE 复合副作用的准确归类；
- PARTIALLY_APPLIED 的持续写入阻断；
- 人工纠正 operation 的无空窗锁转移；
- SET_ONLINE 全目标异常的 0 写入批次阻断；
- SET_ONLINE 两阶段批次中断后的逐商品副作用分类；
- Review 对不同动作的统一阻断矩阵；
- 快照、异常、Review 和通知的事务一致性；
- 任务11/12库存投影行为保持不变；
- v12→v13 无损迁移和任务12回归门禁；
- Worker、phase、Importer 和 Watchdog 的职责分离；
- 现有 review_tasks、v4 operation 和 v4 批次的兼容迁移；
- operation、Task、Batch 和锁状态的明确投影；
- 写操作 UNKNOWN、写锁和唯一 RECONCILE 的安全要求；
- 任务13逐步增强但不阻断任务12现有能力。
